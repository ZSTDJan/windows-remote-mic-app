# Changelog — 无线麦 RC003 (Windows)

候选阶段使用持续递增的 `candidate.N` 测试序号。内部构建版本号唯一来源是
`src/ovb_rc003/VERSION`（当前 `1.0.72`），程序、冻结构建和
包装都读取该文件。新发布 tag 为 `v<VERSION>`，历史 tag 与包名保持原样。

正式版把最终采用的候选序号放在最后一位：采用 `candidate.30` 时发布为 `v1.0.30`，
后续候选继续从 `candidate.31` 递增。正式 tag 不带 `windows`、`rc003` 或 `candidate`。
完整命名规则以 `VALIDATION-AND-DELIVERY.md` 的“版本与命名规则”为准。

## [1.0.72] — 2026-09-24

- 采用第 72 次候选源码发布正式版；按键、语音及图标沿用以下已完成改动。
- 不提供滚轮向上／向下映射入口。`PageUp`／`PageDown` 在高权限浏览器窗口的遥控器实测尚未通过，不将其写为已解决。

## [1.0.72-candidate.72] — 2026-09-24

- 修正 `PageUp`／`PageDown` 映射的 Windows 输入：按物理翻页键扫描码和扩展键标记发送。
  `.71` 的实体遥控器测试中，方向上映射已触发、Windows 接受模拟按键，但用户看到
  页面未滚动；本次修正后的目标程序效果仍待实测。
- 应用窗口、任务栏和通知区域改用维护者已定稿并授权公开分发的蓝色麦克风图标，
  Windows 程序图标由同一已定稿 SVG 生成。
- 按用户决定移除语音页“试输入／语音试说”及应用内输入框；声音通道检查仍保留，
  语音文字是否上屏改在实际目标程序中验证。

## [1.0.71-candidate.71] — 2026-09-24

- 延续 `.70` 候选的小米与谷歌 Chromecast 遥控器按键映射、语音及界面改进。
  `.70` 本机日志确认两款遥控器的多轮收音与结束，用户确认界面和语音文字上屏正常。
- 移除滚轮向上／向下映射；已保存的这两种动作在新版中分别按 `PageUp`／
  `PageDown` 执行，配置文件要在用户下次保存时才改写。翻页键作用于当前焦点，
  不能保证解决特定程序对模拟输入的拦截；真实效果仍待验收。

## [1.0.44] — 2026-09-14

- 2026-09-14 更新 GitHub 首页使用说明：管理员权限提示移至使用限制，明确受管内网
  环境可能拦截权限、虚拟音频、按键与联网功能，并补充游戏反作弊识别风险；新增设备
  页全绿状态和语音程序、虚拟麦克风、长按快捷键对应关系的图文配置说明。

- 2026-09-14 candidate.44 已通过本机验收，按项目连续序号规则采用为首个正式版
  `1.0.44`。正式源码、下载入口与 `v1.0.44` 统一，发布 ZIP 和校验文件由同一次
  Windows CI 生成；candidate.44 的历史 ZIP、哈希和记录保持不变。

- 2026-09-14 已导出 candidate.44 本机编译 ZIP，纳入 candidate.43 导航诊断及
  后续自身窗口接收修复，仅按既有边界编译两个权限模块。修正一处仅在完整测试
  入口暴露的测试导入路径；全量复验、编译后检查、源码一致性和解压启动检查通过。
  该包只作为正式版采用前的本机验收记录；见 `CHECK-DESKTOP-037`。

- 2026-09-14 修复无线麦自身窗口的元素导航接收：映射动作可直接进入导航队列，
  窗口键盘事件增加 Qt 接收，避免完全依赖系统钩子；接受后不重复发键，保留长按、
  松键与快捷键录入优先级。开发版检查点待实测，微信空扫描问题尚未修复，未打包。

- 2026-09-14 candidate.43 增加元素导航定向诊断：补齐导航键进入监听后的处理
  分支、返回值和异常，并按标准控件类型汇总扫描排除原因；不改变按键、扫描和
  导航规则。本轮使用开发版继续定位无线麦自身窗口不移动及微信扫描为空，未打包。

- 2026-09-14 修复本机双遥控下来源入口识别失败：从 Windows 映像导入表直接定位
  按键函数，避免 Frida 返回相邻函数槽时误判为无法区分来源，并补充入口状态日志。
  现场只读核对及 106 项检查通过；纳入 candidate.42 本地免编译测试 ZIP，并把
  管理员助手代际升至 13，确保已装旧助手会更新按键脚本。真实双遥控改键仍待验收。
  candidate.41 在交付前发现升级路径不完整，已废弃且不交付。

- 2026-09-14 candidate.40 汇总已完成的二级弹窗/普通试输入、元素导航诊断日志，
  并收口此前微信主动刷新读取设置及用户配置流程修改。豆包远程无法唤起暂列已知
  限制，不强制绕过输入环境检查；微信远程自动上屏、元素导航故障仍待人工验证。
  已生成未签名本地免编译测试 ZIP，实际冻结入口与包装校验通过；追加核查豆包
  新机进程漏检修复，未发现新回归。交付结果见 `CHECK-DESKTOP-034`。

- 2026-09-14 已生成 candidate.39 本地免编译测试 ZIP，纳入输入法窗口、麦克风
  使用历史、剪贴板来源、粘贴候选按键、语音状态及声音测试服务恢复记录。
  实际冻结程序、Qt、助手、源码/QML 和 ZIP 校验通过；微信与豆包远程问题仍待
  真机日志定位，见 `CHECK-DESKTOP-033`。

- 2026-09-13 已生成 candidate.38 本地免编译测试 ZIP，包含进程权限、焦点、
  剪贴板序号诊断及“导出日志”入口。实际冻结程序、Qt、助手、相关源码/QML 和
  ZIP 校验通过；远程输入与双遥控等实体效果仍待实测，见 `CHECK-DESKTOP-032`。

- 2026-09-13 原“日志目录”链接改为“导出日志”，选择保存位置后后台生成 ZIP，
  包含已有日志、轮转备份和诊断报告；不包含配置或录音。提供空日志、部分读取
  和保存失败提示，保留原始日志；相关检查通过，见 `CHECK-LOG-EXPORT-001`。
  已进入 candidate.38 本地测试包。

- 2026-09-13 现有诊断日志补充各程序实际权限、剪贴板变化序号和语音结束后
  10 秒的焦点观察，限时接收入口复用同一采集；不读取剪贴板内容，不增加选项。
  修正搜狗语音开始记录的提供方标签。受影响检查通过，远程气泡与上屏仍待实测，
  已进入 candidate.38 本地测试包，见 `CHECK-SUBMISSION-LOG-001`。

- 2026-09-13 已生成 candidate.37 本地免编译测试 ZIP，包含设备实例身份读取修复
  与通用程序/限时接收诊断；实际冻结程序和 ZIP 校验通过，实体效果仍待实测。
  来源和交付证据见 `CHECK-DESKTOP-031`；旧包及历史记录保留。

- 2026-09-13 修复双遥控来源身份读错注册表层级：从已验证设备的参数句柄取得
  自身实例根 ContainerID，保持只接收已登记且当前选中的设备。112 项相关检查
  通过，双遥控真机及升级仍待测，见 `CHECK-HID-SOURCE-003`。

- 2026-09-13 已生成 candidate.36 本地免编译测试 ZIP，包含本次日志增强及
  未适配设备实际名称。冻结程序与 ZIP 检查通过；双遥控与偶发语音故障仍待
  实机定位，见 `CHECK-DESKTOP-030`。旧批次的“未更新 ZIP”为历史状态。

- 2026-09-13 通用日志增加前台程序文件名、窗口/焦点及键盘布局变化，语音快捷键
  按每个键记录请求与原生调用结果；新增不启动服务的限时只读接收入口。不限定
  UU，不改变发送/输入法/设备行为，不把收到按键写成语音成功。源码检查通过，
  既有 .36 ZIP 未更新，见 `CHECK-PROGRAM-LOG-001`。

- 2026-09-13 补齐设备来源与语音故障日志：实际按键脚本身份、设备身份读取层级、
  右 Alt 按下/松开确认和前台线程；开启现有诊断日志后自动限量保存故障前后报告，
  服务重启后仍可反馈。无新增操作，不放宽设备选择；双遥控和偶发语音故障仍待
  定位，见 `CHECK-DIAGNOSTIC-001`。

- 2026-09-13 已生成 candidate.35 未签名本地测试 ZIP，包含双遥控实际调用入口、
  豆包运行进程漏检和设备下拉名称修复。直接封装 Python 源码，未执行 Cython；
  实际主程序/助手自检、冻结代码与 QML 一致性及 ZIP 校验通过，实体效果待测。
  详见 `CHECK-DESKTOP-029`；下文“未更新 ZIP”保留为对应源码批次的历史状态。

- 2026-09-13 按用户确认显示未适配蓝牙设备的实际名称，并标注“暂不支持”；
  缺少名称时才显示短编号。列表名称按纯文本显示，不改变可添加范围、设备选择
  或配置格式；相关扫描与真实 QML 检查通过。现有 .35 ZIP 未更新，见
  `CHECK-DEVICE-LABEL-001`。

- 2026-09-13 修复双遥控共用驱动进程时的来源识别入口：按宿主实际导入槽取来源，
  覆盖系统转接和兼容入口，保留“只接收已登记且当前选中的设备”及来源不明拒收。
  112 项相关检查通过，双遥控实体效果待实测；界面、配置和既有 candidate.34 ZIP
  均未改变，见 `CHECK-HID-SOURCE-002`。

- 2026-09-13 修复豆包正在运行却被语音组件漏检：改用 Windows 进程枚举查找服务，
  继续执行原有模块和版本校验；诊断工具统一复用正式唤出准备流程。相关 85 项检查
  通过，测试机实际气泡和音频仍待测。未更新现有 ZIP，见 `CHECK-DOUBAO-DISCOVERY-001`。

- 2026-09-13 修复 candidate.34 的设备下拉展开后显示内部对象名称：弹出项按显示角色
  读取设备名，空列表注明先添加设备；设备与语音页在旧组件迁移受阻时明确提示重启电脑
  后重新打开程序。沿用原按钮、操作步骤和设备选择规则，未修改底层迁移策略。
  真实 QML 回归通过，源码为检查点待实测；原测试 ZIP 未更新，见 `CHECK-DEVICE-SELECT-002`。

- 2026-09-13 00:40 已生成 candidate.34 未签名本地测试 ZIP，包含具体遥控器选择、
  共享宿主按键来源识别与当前工作区已有配置修复。直接封装 Python 源码，未运行 Cython。
  主程序、按键助手自检及 ZIP 内容校验通过，两端冻结代码与源码一致；双遥控仍待实测。
  见 `CHECK-DESKTOP-028`；下文“未构建新包”只描述对应源码批次，旧包保留。

- 2026-09-13 源码补充共享驱动进程内的实体按键来源识别：只处理已入档且选中的设备，
  其它来源不能抢占候选或进入按键原文诊断；来源不明时保持连接并明确提示，不循环重启。
  助手代际 12、来源能力版本 1，保留单设备兼容路径和旧组件安全迁移限制；不增加界面
  操作。隔离与真实 QML 检查通过，双遥控实机为检查点待实测，见 `CHECK-HID-SOURCE-001`。
  未构建新包，既有 candidate.33 不含本条及前一轮设备选择功能。

- 2026-09-12 源码增加按具体遥控器选择：主页面不增加行，设备页第一行打开管理弹窗；
  按键、检测、语音和重连统一使用所选设备，切换先停止旧输入，移除不删除 Windows 配对。
  旧配置保留映射但首次需选择，未适配型号不能启用。共享驱动进程时明确提示来源尚不能
  区分，不再反复让用户按方向键或统一要求重启电脑。固定助手代际升至 11，只读并严格
  校验所选设备摘要，独占宿主保护不变。自动检查通过，实体设备为检查点待实测；
  见 `CHECK-DEVICE-SELECT-001`。未构建新包，既有 candidate.33 不含本条改动。

- 2026-09-12 按键运行组件支持受保护脚本热重载，修复断线后空数据循环。助手代际 10
  会在安全条件满足时单独重载旧 RC003 驱动实例，确认新宿主后重新接收按键；无法确认
  独占在线设备或重载失败时保留重启提示，不强杀系统进程。自动检查通过，真机待测，
  详见 `CHECK-HID-RELOAD-001`；重启电脑不再是新版升级的必做第一步。

- 2026-09-12 20:31 已生成包含上述修复的 candidate.33 未签名本地测试包，旧包保留。
  冻结主程序与助手均已核验修复代码一致，基础自检通过，实体设备仍待用户验证；
  来源与校验见 `CHECK-DESKTOP-027`。

- 2026-09-12 14:25 已生成 `candidate.32` 未保护本地测试 ZIP，纳入旧按键助手升级、
  配置流程修复及当时工作区其它最新改动。直接从 Python 源码封装，未运行 Cython；
  产物自检和 ZIP 校验通过，人工待测，详见 `CHECK-DESKTOP-026`。下文“尚未打包”
  仅描述对应原修复批次，旧 `candidate.31` 包未改写。

- 2026-09-12 配置流程审计修复：运行中改输出端点后保留已有“重启服务”入口；缺少语音
  快捷键不再阻挡普通映射自动保存与恢复默认；语音程序在后台启动，避免阻塞设置窗口；
  音频安装指引改为实际按钮名称，并修正模块导入及文档/公开边界检查缺口。完整自动检查
  通过，真实设备仍为检查点待实测（`CHECK-CONFIG-FLOW-001`）；没有更新已有测试包。

- 2026-09-12 源码修复旧按键助手升级遗漏：助手代际升至 9，使仍携带协议 3 的代际 8
  助手进入现有更新授权流程；运行组件版本不匹配时保留“请重启电脑”提示，等待新设备
  宿主后恢复接收，不再反复提示按方向键。两页提示与隔离恢复流程已验证，异机升级仍为
  检查点待实测（`CHECK-HID-UPGRADE-001`）；既有 `candidate.31` ZIP 不包含本修复。

2026-09-12 已生成 `candidate.31` 未保护本地测试 ZIP，包含当时工作区的 HID 提前拦截、
诊断与语音修复，以及微信主动刷新读取设置的未提交改动。跳过 Cython，直接从 Python
源码封装；来源、检查结果和哈希见 `TESTING.md` 的 `CHECK-DESKTOP-025`，真实设备待实测。

状态校核：2026-09-13。源码版本值为 `0.2.0-candidate.34`；2026-09-10
的 `candidate.30` 本地测试包检查只证明当时内容，不包含随后新增的源码修改。
2026-09-11 本机已通过应用切换连击、方向跳格与菜单双触发修复：HID 协议 4
在 Windows 复制原报告前处理 usage，不再依赖下游方向钩子的到达顺序。
失败日志已补充分因和版本指纹，重载实测仍待完成；跨机与重连也不能继承本机结果。
上述修复的原批次没有生成新包或发布。具体范围见 `CHECK-HID-COPY-001`、`CHECK-HID-LOG-001`。
当前源码已合入三种输入法快捷键自动读取与录入兜底、微信真实开麦确认、语音欠松重试、
真实运行状态和 `Win+H` 产品入口撤下等修复。`candidate.21`
的实机结果继续有效：启动后搜狗窗口不再自行常驻，遥控器语音键按下可唤起、松开可收回；
`candidate.30` 本地包是该批次的测试入口，不代表当前开发版；通知区域和真实设备流程仍按对应批次验收。
部分历史真机路径通过；
HID 失联补松、单进程生命周期、旧版安全升级、无 UAC 登录自启和
预授权助手流程仍待当前源码对应产物真机复测。
历史测试已经确认 13 个实体键可识别、普通映射可执行、按住话筒键时存在
有效语音数据和可见识别文字，并解决了日期时间漏出、检测抢占、设置热加载、
短音频乱序及松键释放等问题。最后一条隔离协议探针完成协商后仍未收到实体
`START_SEARCH`，普通按钮也只能唤起宿主而不能让遥控器传声。用户据此决定正式
移除用户可选的开关型语音和非话筒键语音：当前只支持实体话筒键按住说话，旧配置
不会被自动改成另一种实体操作。通用“松手后再触发一次”已撤下；微信和豆包输入法会先
激活各自输入法，再发送当前保存的按住型快捷键。设置页可读取微信隐藏状态栏提示和豆包
用户配置中的当前值，读取失败时仍可手动录入；当前不自动切回原输入法。
自定义组合键也不再随长按
定时重复。本节不得扩大为当前候选已完成真机验收
的声明。
维护证据与验收入口见
`MAINTENANCE.md`、
`bugs/BUG-001-audio-endpoint.md`、`bugs/BUG-002-hid-tap-readiness.md`
、`bugs/BUG-003-duplicate-bridge-dialog.md`、
`bugs/BUG-004-background-bridge-tray.md`、
`bugs/BUG-005-toggle-continuous-voice.md`、
`bugs/BUG-006-live-bridge-key-detection.md`、
`bugs/BUG-007-duplicate-winrt-ble-candidates.md`、
`bugs/BUG-008-toggle-multi-source-gesture.md`、
`bugs/BUG-009-f5-hook-release-race.md`、
`bugs/BUG-010-live-voice-settings-reload.md`、
`bugs/BUG-011-mic-key-detection-voice-race.md`、
`bugs/BUG-012-toggle-reopen-before-physical-release.md`、
`bugs/BUG-013-key-detection-claim-race.md`、
`bugs/BUG-014-toggle-reopen-f5-echo.md`、
`bugs/BUG-015-sogou-codex-text-commit.md`、
`bugs/BUG-016-audio-stop-overtakes-audio.md`、
`bugs/BUG-017-toggle-firmware-audio-boundary.md`、
`bugs/Alt键锁死问题.md`、
`bugs/BUG-019-stale-candidate-probe-hangs-instance.md`、
`bugs/BUG-020-direction-hold-repeat-leak.md`、
`bugs/BUG-021-hold-only-voice-product-boundary.md`、
`bugs/BUG-022-custom-combo-hold-repeat.md`、
`bugs/BUG-023-host-voice-release-finish.md`、
`bugs/BUG-024-settings-edit-state-and-stale-hold-config.md`、
`bugs/BUG-025-bridge-startup-readiness-warning.md`、
`bugs/BUG-026-bridge-liveness-and-first-key-readiness.md` 和 `TESTING.md`。

### 2026-09-10 candidate.30 未保护快速测试包

- 最新输入法保持与豆包探针改动进入本地测试包；本次按用户测试需求跳过 Cython
  源码保护，直接从当前 Python 源码执行 PyInstaller 冻结，不作为正式候选或发布包。
- 修正 PyInstaller 对构建环境 `PATH` 的隔离：除原有外部 `icuuc.dll` 外，同时排除
  外部 `icuinXX.dll` 和 `icudtXX.dll`。此前 `candidate.29` 误收集的
  `icudt78.dll` 来自本机 Poppler 工具链，不是程序运行依赖。
- 输入法与应用接线定向测试、ICU 打包规则测试和 23 项版本/用户文档合同通过；冻结
  主程序 `--dry-run`、HID 助手 `--self-check` 和 Qt 运行时检查通过。包内没有 ICU DLL。
- 未签名本机测试包为 `无线麦便携测试包-candidate.30-未保护.zip`，大小
  149,098,226 字节，SHA-256 为
  `A57194F4AD01CDD4CEE4B10C8EDF94C9768949DA299E4235123F0C87246FE22E`；包内主 EXE
  SHA-256 为 `A9F4D955B4D1F898BEA720EFB3C53C45847D9673EC0DC51A3AED2115664DCD4B`。
  ZIP 只有一个 `RemoteMicRC003-0.2.0-candidate.30` 顶层目录，共 2123 个文件。

### 2026-09-10 微信长按容错与豆包设备层探针

- 微信输入法的会话级激活或查询失败时，仍尝试发送用户保存的快捷键；开麦确认改为
  2、3、5 秒恢复阶梯。全部确认失败只记录诊断结果，不再在实体话筒键仍按住时强制
  松开快捷键、关闭 RC003 音频或停止转发，正常松手继续负责完整收尾。
- 新增独立的豆包 Interception 右 Alt 探针，只匹配 RC003 键盘设备，默认只读枚举，必须
  显式传入 `--send` 才发送成对按键；它不进入程序运行链路、不安装驱动或修改配置。
- 输入法控制与应用接线定向测试 68 项通过、1 项按平台条件跳过，Python 语法和差异格式
  检查通过。本机加载 Axonkey 的 Interception DLL 后无法创建驱动上下文，故未发送豆包
  测试按键；微信、豆包真实输入和文字上屏均为检查点待实测。本轮不构建、不打包、不发布。

### 2026-09-10 candidate.29 用户反馈修复便携测试包

- 本包在 `candidate.28` 基础上纳入微信未确认开麦不再错误重连、空手动快捷键恢复自动读取、
  搜狗启动不再提前唤起语音窗口，以及豆包快捷键读取从当前已核验版本提取真实调用合同。
- 首次完整构建正确拦截了安装说明中残留的 `candidate.28` 示例文件名；修正为
  `candidate.29` 后版本一致性单项和完整构建通过。最终完整 unittest 2670 项通过、7 项按
  环境跳过，编译态权限模块 136 项通过、1 项跳过；公开边界、第三方通知、Cython、
  PyInstaller、冻结主程序、HID 助手和 Qt 运行时检查全部通过。
- 来源为提交 `36adc93` 加构建时的安装说明一行修正；构建输入 220 个文件，来源指纹
  SHA-256 为 `11F379FBAD35429AFC1D9F520C8A24401C5739136D20D8DF6657000CB7975A8C`；
  构建产物指纹为 `6C7A4B468A8003B4A1C5B56BA381C61D4080E697D0ED24D98D153B3050CD28A6`。
- 未签名本机测试包为 `无线麦便携测试包-candidate.29.zip`，大小 163,169,293 字节，
  SHA-256 为 `5E4EA6A3D602626960D5C59F53697F3553EB126F8AF3EB0AA327778E5C9E9A4A`；
  包内主 EXE SHA-256 为 `90E9CB2F49ECB6C9B5E38EA53E8BB077300CE718416A3627F0463D10A2FBB7E9`，
  内部 HID 助手为 `FC9B4FFD26EDB3BBFE539602F9BDC350DB859841A2F1F9D85466EB86CA19BBE0`。
  ZIP 只有一个 `RemoteMicRC003-0.2.0-candidate.29` 顶层目录，共 2126 个文件，包内版本正确。
  真实 RC003、搜狗、微信、豆包和远程窗口仍待人工实测。

### 2026-09-10 candidate.28 用户反馈修复检查点

- 日志确认 `candidate.28` 在微信未确认开麦后把一次语音失败误升级为蓝牙重连；当前只关闭
  本次语音、释放快捷键并禁止继续转发，不再因此重连。日志同时存在非零 PCM，故障不在
  遥控器收音链。
- 空的 `{"hold":"","source":"manual"}` 以前仍占最高优先级，自动读取失败后又触发
  空快捷键校验。当前空值自动降为默认来源；即使自动读取失败，也能保存“已选择、待录入”
  状态。合法手动值仍优先，普通自动读取不能覆盖，界面明确显示“自动识别”或“手动录入”。
- 删除搜狗 `--auto-launch` 和可见窗口检查。真实按下前只短暂确认搜狗进程；失败时不发送
  快捷键和 `MIC_OPEN`，避免预热产生没有对应松手的自由说话窗口。
- 豆包普通合成键会被自身低层键盘回调拒绝。当前只对已核实的 `ImeService.exe` 版本，在
  其回调内处理无线麦专用标记边沿；未知版本直接失败关闭。输入法 STA 超时后的任务会在
  每次切换后立即检查取消并恢复原输入法，不能继续迟到切换。
- 当前源码编译、隔离 `--dry-run`、按下/抬起/补松/退出清理 327 项，以及设置页、QML、
  快捷键读取和程序管理 482 项均通过，1 项按平台跳过。未构建新包，状态为检查点待实测。

### 2026-09-10 candidate.28 当前修改便携测试包

- 本包包含 `candidate.27` 之后的三种输入法快捷键自动读取与录入兜底、微信真实开麦确认、
  语音欠松重试、真实运行状态、`Win+H` 产品入口撤下、按键映射自动保存，以及当前工作树中
  已纳入构建输入的方向键与元素导航修正。
- `build/build-candidate.ps1` 完整入口通过：公开边界扫描 463 个文件，完整 unittest
  2663 项通过、7 项按环境跳过，编译态权限模块 136 项通过、1 项跳过；第三方通知、Cython、
  PyInstaller、冻结主程序、HID 助手和 Qt 运行时检查全部通过。
- 来源为提交 `551f5b7` 加构建时的未提交工作树；构建输入 220 个文件，来源指纹 SHA-256 为
  `E07A8BE26D66567A0DCFF943CE380C6351AD08FC65804523B52D0670AA07E64B`；构建产物指纹为
  `967E0E1113F86DBFDFB419BB9D73BFCE32116901E3BFE727D1F8990CEDE94388`。
- 未签名本机测试包为 `无线麦便携测试包-candidate.28.zip`，大小 163,166,392 字节，
  SHA-256 为 `F02F1010FA55AB12DDC1B42BFBB8ADCDB49D4FE6C1F48981DF50E62C3802AF80`；
  包内主 EXE SHA-256 为 `00F9C676A68AE0E440A498280FCA4775483F4A691E12D48F045C21EDD875B6D4`，
  内部 HID 助手为 `3283BFF3E3CBA2450DC497CA66343832E575E58F34B095F57B7FE1AE41CC6D4C`。
  ZIP 只有一个 `RemoteMicRC003-0.2.0-candidate.28` 顶层目录，共 2126 个文件，包内版本为
  `0.2.0-candidate.28`。真实 RC003、三种输入法文字上屏、方向键和自动保存流程仍待人工实测。

### 2026-09-09 三种输入法唤出、真实状态与收尾完整修复

- 搜狗、微信和豆包分别自动读取自身按住型快捷键，并统一保留普通白色“录入”按钮。
  手动录入值优先；只有用户主动刷新且读取成功时，才用输入法当前值覆盖。配置 schema
  升到 13，记录默认、自动和手动来源，显式留空的手动值不会被默认值顶回。
- 微信和豆包运行时从系统已安装输入法列表动态识别对应 TSF profile。独立 STA 操作超时
  会取消并等待线程收尾，避免本轮已经失败后后台又迟到切换。微信按键发出后读取麦克风
  `LastUsedTimeStart`，首次未确认只循环输入法并重发一次，仍未确认就释放按键、停止转发
  并显示“语音程序未响应，程序已禁止开麦”。
- 搜狗发送快捷键前先确认目标窗口；失败时不发快捷键、不发开麦命令。搜狗和自定义程序
  启动后只在短截止时间内确认真实进程，不持续轮询。未经真实首帧 PCM 不再显示“正在接收”，
  `AudioStarted` 初始化失败会完整回滚会话状态。
- 删除 Windows 语音输入/`Win+H` 产品入口和诊断项，旧配置迁移到“不管理”。设置页删除
  “气泡已打开”“上次正常”等无法证明的状态，只保留配置、真实开麦、真实 PCM 和明确失败。
  刷新图标移入输入框末尾，640/720 紧凑窗口仍保留可用输入宽度；豆包右 Alt 不再被微信
  专用物理化链路误拦截。
- 语音按键释放失败使用一个退避定时器继续补松，新会话开始前先清理旧欠松；正常断连、停止
  和完全退出继续补释放。主程序被强杀时无法保证执行清理，仍作为极低概率已知风险保留，
  本轮不增加外部看门狗。最终完整 unittest 2663 项通过、7 项按环境跳过，结束后无资源泄漏
  告警；公开边界扫描 463 个文件和差异格式检查通过。本轮不构建、不打包、不发布，状态为
  检查点待实测。

### 2026-09-09 按键映射自动保存

- “按键”页删除单独的“保存映射”按钮。编辑弹窗点“完成”后自动保存主动作、双击、
  长按和备注；“恢复内置默认”确认后也立即保存，与语音页现有自动保存操作保持一致。
- 同一编辑弹窗产生的多项变化合并为一次原子写入；桥接启动、端点预检、驱动操作、
  声音测试或语音快捷键写入占用期间延后保存。保存期间继续修改会废止旧候选并补存最新
  revision；写入失败保留界面修改，并在底部显示“重试保存”。
- 正常退出遇到待保存映射时先保存再退出；编辑草稿未完成、其它设置未保存或自动保存失败
  时仍保留保存、放弃和取消出口。受影响范围 684 项通过、1 项按平台条件跳过；本轮不构建、
  不打包，真实 RC003 编辑、重开和退出流程仍为检查点待实测。

### 2026-09-09 任务切换与元素导航方向单次归属

- `candidate.27` 日志确认任务切换器一次跳两格不是遥控器重复或长按连发，而是软件方向
  映射后，Windows 同一次原始方向键又迟到执行。此前元素导航的精确边沿去重方向正确，
  但只在导航开启时生效。
- 当前源码把 RC003 方向边沿归属移到进程内既有低层键盘钩子，按虚拟键、扫描码、扩展
  标志和按下/松开精确匹配，并覆盖同一次按住重复。设备完整松开早于 Windows 原始边沿时，
  待匹配按下仍会短时保留；只有钩子确实吞过按下才会吞对应松开，避免反向乱序造成卡键。
  软件注入方向和未登记的普通电脑键盘不受影响，元素导航原逻辑保留为全局钩子不可用时的兜底。
- 相关 617 项自动检查通过、1 项按环境跳过；当前尚未构建新测试包，真实任务切换和元素
  导航仍待 RC003 实测。`candidate.27` 不能作为本项通过依据。

### 2026-09-09 设置页状态提示统一

- 设备、按键和语音页的黄色或红色状态改为直接写出下一步按钮或动作；窗口底部常驻提示
  用户按状态文字处理，直至相关项目变为绿色。“实际说话”会明确提示打开设备、重新录入、
  重新检查、启动程序或试说一句，不再只显示笼统的待处理状态。
- 桥接运行状态协议升级到 schema 3，新增“正在连接”和“连接失败等待重试”，并继续兼容
  读取 schema 1/2。连接失败时设备页显示“点击‘重新连接’”，语音页引导打开设备页处理，
  不再误提示用户反复按方向键。
- 受影响范围 324 项执行时仅剩 1 个旧按键检测文案断言，更新后单项复测通过；Basic 与
  FluentWinUI3 的状态矩阵、640×480 和 720×560 等紧凑窗口布局均通过。未构建、未打包，
  新文案和真实 RC003 连接失败场景仍为检查点待实测。

### 2026-09-09 豆包输入法与微信快捷键自动读取

- “语音程序”下拉新增“豆包输入法”。程序会从运行进程、卸载登记和标准安装目录自动
  识别豆包，提供自身设置入口；豆包由无线麦直接适配，不归 Windows 管理，也不作为普通
  程序随遥控器服务启动。
- 豆包从 `%APPDATA%\DoubaoIme\conf\config.json` 的
  `voice.voiceLongPressShortcut` 读取按住型快捷键；微信从隐藏的
  `wetype.statusbar.window` 无障碍按钮提示读取当前按住型快捷键。读取失败均保留上次值和
  手动录入兜底，不改写两种输入法的私有配置。
- 搜狗、微信和豆包的语音按键输入框右侧增加 28 像素刷新图标，输入框相应缩短。刷新成功
  立即保存并从下一次语音生效；录入、保存、正在说话或收尾时按钮禁用，后台槽位也拒绝
  执行。没有增加窗口重新激活时自动刷新。
- 本机只读核实已识别豆包安装和运行状态，设置入口为安装目录内
  `bootstrap\SettingsLauncher.exe`；当前豆包读取为右 Alt，微信读取为左 Ctrl + 左 Shift + F9。
- 后续自检直接核对豆包设置程序集的 `ToBindingModifierFlags`，补齐左/右 Ctrl、Alt、Shift、
  Win 的真实编码，并拒绝无法识别或左右标记与基础标记不一致的配置。刷新在开始读取和准备
  保存前都会重新同步运行状态；状态不可确认、正在保存、录入、说话或收尾时均放弃本次刷新，
  图标同时显示为不可用。
- 扩大受影响范围共运行 960 项，959 项通过、1 项按平台跳过；本机真实配置仍可读取豆包右 Alt
  和微信左 Ctrl + 左 Shift + F9，源码编译和工作树、暂存区差异格式检查通过。
  当前源码尚未构建新测试包，真实 RC003 文字上屏和无残留按键仍待实测。

### 2026-09-09 candidate.27 微信与元素导航修正测试包

- 本包在 `candidate.26` 后纳入微信输入法独立 STA 激活与固定按住型
  `Ctrl+Win`、RC003 方向边沿归属去重、元素导航整窗大容器过滤和首次移动补扫。
- 首次构建在编译前被公开边界扫描阻止：新增的外部参考已正式写入
  `ATTRIBUTION.md`，但扫描器仍把该归属文件当成普通源码。现已严格限定只允许
  `ATTRIBUTION.md` 保留准确的外部仓库名，其它文件只链接归属记录。
- `build/build-candidate.ps1` 唯一完整入口通过：unittest 2593 项通过、7 项按
  环境跳过；公开边界、第三方通知、资源释放、Cython 编译态权限测试、PyInstaller、
  冻结主程序、管理员 HID 助手和 Qt 运行时检查全部通过。
- 来源为提交 `551f5b7` 加构建时的未提交工作树；构建输入 220 个文件，来源指纹
  SHA-256 为 `CEB6F713D16DD10C8F9B99F4DBA31CD77EFD3B6C531298536D55F668846A5A48`；
  构建产物指纹为 `11E602F9565203D3A5EC0515BD591A21793E0BC583C9BB92E1592CECD819C70C`。
- 未签名本机测试包为 `无线麦便携测试包-candidate.27.zip`，大小 163,138,943 字节，
  SHA-256 为 `29CA9D25DABE811AB03F1DE4B6D259DBF9229FE0DAA617A9BA8B0DE56E8141EB`；
  包内版本和唯一顶层目录均为 `0.2.0-candidate.27`，共 2126 个文件。真实 RC003、
  微信本地/远程语音、方向点按/连按/长按和大容器进入仍待人工实测。

### 2026-09-09 微信输入法按住型快捷键唤出修正

- `candidate.26` 用户日志确认：本地普通输入框 4 次均能打开微信语音；远程窗口
  `Qt51512QWindowIcon` 中 8 次均因 WeType 状态栏按钮隐藏而失败，程序随后主动禁止开麦。
  旧实现实际查找 `wetype.statusbar.window` 并向固定坐标投递鼠标消息，不是微信底层语音接口。
- 对照 [`ATTRIBUTION.md`](ATTRIBUTION.md) 已登记的 GPL Windows 参考实现后，微信运行入口改为独立 STA 会话级激活
  WeType；已是微信输入法时立即继续，冷切换等待 50 毫秒。`CoInitializeEx` 失败时不再在
  错误的 MTA 套间继续调用 TSF。
- 微信默认使用左 `Ctrl+Win`，设置页恢复按住型快捷键录入；无线麦只保存自己的组合键，
  不读取或改写微信输入法私有配置，用户需确保两边一致。运行时逐边沿发送当前保存的组合键，
  松手释放同一会话实际按下的键。
- `SendInput` 成功即允许音频继续，不再以状态栏或气泡可见作为开麦条件。部分发送、发送异常、
  断连、停止和退出继续执行回滚、补释放与清理重试。其它语音程序和普通按键映射不变；当前
  源码不自动切回原输入法，也未构建或打包新候选。

### 2026-09-08 TV 键保留式应用切换

- “应用切换”动作由 `Alt+Tab` 改为 `Ctrl+Alt+Tab`。TV 默认映射和其它选择该语义
  动作的按键都会打开并保留 Windows 任务切换器，之后可用方向键选择并按确定键进入，
  不再依赖持续按住 Alt。
- 配置仍保存为原有 `app_switcher` 语义动作，不改变配置格式或已有映射；异常补释放已
  同步覆盖 Ctrl、Alt、Tab。相关 146 项定向测试通过，真实 RC003 与 UU 远程操作仍待实测。

### 2026-09-08 candidate.26 今日修改与便携测试包

- 本包在 `candidate.25` 基础上纳入 Win 系统快捷键手动输入、按键编辑草稿前置校验、
  重复打开后的窗口隐藏恢复、撤下遥控器实体组合映射、多便携版本全局移除管理员按键
  权限，以及设备页“请按方向键 / 已连接”状态修正。
- 完整构建首次运行时发现两处今天新增功能后的旧测试口径：提示框测试在鼠标移开后才
  读取可见性，按键检测文案测试同时要求和禁止旧条件表达式；已只修正测试取值与断言，
  未改变实际界面行为。
- `build/build-candidate.ps1` 唯一完整入口通过：unittest 2577 项通过、7 项按环境跳过；
  公开边界、第三方通知、资源释放、Cython 编译态权限测试、PyInstaller、冻结主程序、
  管理员 HID 助手和 Qt 运行时检查全部通过。
- 来源提交为 `1ebb6c2`；构建输入 220 个文件，来源指纹 SHA-256 为
  `7BA887706441BAA52E4287A3B56D9D620C60A001D9B9308C3C5967637DF23EBF`；构建产物
  指纹为 `9002AFB2637BB4C8B9AA5C8E3A6906DA6AD9A9D8B87BF589DD768BFE5BADF913`。
- 未签名本机测试包为 `无线麦便携测试包-candidate.26.zip`，大小 163,141,318 字节，
  SHA-256 为 `76681D29EB4A771D5BC768C41F0BD86CA2B9B75C138072C50394BFC62B4661DB`；
  包内版本和唯一顶层目录均为 `0.2.0-candidate.26`，共 2126 个文件。真实 RC003、
  系统快捷键、权限移除、休眠唤醒、微信识别与文字上屏仍待人工实测。

### 2026-09-08 设备页明确区分服务连接与遥控器待唤醒

- 修正设备页把遥控器待唤醒长期显示成“检查中”、把已经连接的遥控器服务显示成
  “确认中”的问题。“确认中”没有独立检查过程，服务已连接时现在直接显示“已连接”。
- HID 按键通道等待遥控器重新出现或首次输入时，按键接收现在显示“请按方向键”；只有
  程序确实正在启动、重连、修复或恢复通道时才显示“检查中”或“连接中”。
- 本轮只修正状态解释和显示，不改变 BLE、Raw Input、HID 接管、用户配置或连接流程；
  真实 RC003 从休眠到唤醒的显示切换仍需人工复测。

### 2026-09-08 多便携版本下可正常移除管理员按键权限

- 修正便携版“移除权限”被旧测试 ZIP 解压目录误拦的问题。程序此前按 EXE 完整路径登记
  每个便携副本，却只检查旧目录是否仍存在，没有检查旧版是否正在运行，因此会把保留在
  磁盘上的旧版本误报为“正在使用”。
- “移除权限”现在按确认框既有说明撤销当前 Windows 账号下所有无线麦版本共用的管理员
  按键组件；其它便携版或安装版以后需要自定义按键映射时，可重新启用并确认 UAC。
  安装器卸载单个版本时仍保留原有消费者保护，不会误删其它便携版依赖的共享组件。
- 每次冻结版登记时会清理已能确认 EXE 不存在的过期使用记录；内容损坏、哈希异常或无法
  可靠读取的记录继续保留，不会被自动删除。本轮只做源码和受影响范围检查，不构建、
  不打包、不发布；真实 UAC 移除与其它版本重新启用为检查点待实测。

### 2026-09-07 管理员组件不可用时的按键安全降级

- 修正一个无权限降级例外：Windows 不认识、但曾学习为逻辑键的实体按键，在 HID tap
  尚未接管时不再执行自定义映射。
- HID tap 未接管时，全部 Raw Input 自定义映射统一停用；Windows 已知键只保留系统原始
  行为和补松保护，未知键不注入操作，按键检测仍可用。HID tap 恢复后映射正常恢复。
- 发布前复核覆盖按键交接、重复与长按、异常补松、语音释放、单实例、自启和管理员助手；
  `candidate.24` 异机日志未发现错误、连击或锁键证据。

### 2026-09-08 Win 系统快捷键手动输入与锁屏执行

- 按键映射的录入弹窗新增“按键录入 / 手动输入”切换；手动模式先停止捕获钩子，再接收
  `Win+L`、`Lctrl+Win+左/右箭头` 等文字格式，避免录入时先触发锁屏、开始菜单或桌面切换。
- 按键映射专用解析支持大小写、全角加号和常用中文按键名；单独 `Win` 按左 Win 保存，
  `Ctrl+Alt+Delete` 明确拒绝。语音快捷键解析和微信“无需设置 / 禁止录入”规则不变。
- `Win+L` 不再把按键事件提交成功当成锁屏成功，改用 Windows 专用锁屏接口；其它 Win
  组合仍使用现有 `SendInput`、失败补释放和重试链路。
- 本轮只执行受影响范围测试、Windows 安全热键探针、Python 语法、QML 检查和 diff 检查，
  不构建、不打包、不发布；真实 `Win+L`、虚拟桌面切换和 RC003 各手势为检查点待实测。

### 2026-09-08 按键编辑弹窗前置校验

- 单击、双击和长按动作在编辑弹窗内复用最终保存的同一套合法性判断；非法内容直接显示
  红色白话提示并禁用“完成”，不会先写入界面模型再等页面底部报错。
- `saveDraft()` 同步增加硬拦截，退出保存等程序入口也不能绕过；用户可继续修改为合法内容，
  或用“取消”和关闭按钮放弃草稿。本轮通过纯逻辑、真实 QML 离屏交互和最小窗口检查，
  不构建、不打包、不发布。

### 2026-09-08 撤下遥控器实体组合映射

- 真机日志从 2026-08-20 到 2026-09-08 共记录 2494 个 RC003 按键边沿，没有一次两个
  实体键同时处于按下；TV+上实测也没有交付重叠边沿。HID 描述符和自动构造报告只能
  证明格式容量，不能证明遥控器固件实际支持。
- 按键页删除遥控器组合映射内容，顶部设备轨道改为选中的“小米遥控器2 Pro”和灰色
  禁用的“其他设备支持中...”，下方只保留 13 键单键映射。
- 旧 `combo_bindings` 数据继续保留用于降级兼容，但当前版本不启用组合识别，不再让
  看不见的旧配置延迟或吞掉单键动作。

### 2026-09-08 按键录入与微信运行状态修复

- 修复 `b3122b9` 引入的按键页录入回归：按键映射使用独立录入入口，不再受微信输入法和
  Windows 语音输入的语音快捷键限制；语音页继续保持微信“无需设置”和禁止录入。
- 微信状态栏控制只点击可见窗口；按下时先通过 Windows 输入服务临时激活 WeType，确认
  状态栏可见后再点击，提交完成后恢复原输入法。旧微信快捷键不再执行；点击或消息投递
  仍不算成功，必须确认同进程“语音输入”面板真实出现。
- 运行状态新增语音程序、最近结果与更新时间。音频结束后先显示“正在收尾”；面板未打开、
  输出端点失败、空音频或收尾失败会回写设置页。只有目标入口已打开、收到非零 PCM，且
  已确认面板关闭和原输入法恢复后才显示“上次正常”。
  程序安装/进程状态、配置就绪和最近运行结果不再共用一个绿色状态。
- 本轮只执行受影响范围测试、Python 语法、真实 QML 离屏检查和 diff 检查，不构建、
  不打包、不发布；真实 RC003、微信识别和文字上屏为检查点待实测。

### 2026-09-08 HID 续租与输出端点状态同步

- HID 管理员组件校验通过后出现的 `gadget_intercept_lease_expired` 不是权限未获取。旧实现
  在同一线程续租并执行按键业务回调，微信面板等较慢操作会拖过两秒租约。续租现由独立
  线程维持，连接关闭时有界停止；业务回调阻塞不再让 HID 接管自行失效。
- 输出端点预检并保存成功后会自动刷新诊断，运行中的旧诊断结果不会覆盖新配置。已经使用
  推荐端点时不再显示“应用”；仅在未使用推荐端点时提供“选推荐端点”快捷入口，下拉选择
  仍会自动预检和保存。
- HID 56 项及设置控制器、诊断、QML 合同和真实离屏渲染 326 项检查通过；本轮不构建、
  不打包、不发布。真实 RC003 长时间按键、微信语音和休眠恢复仍为检查点待实测。

### 2026-09-08 应用图标更新

- 主程序、窗口、安装器、快捷方式和通知区域状态改用新的线框麦克风图标，继续使用原有
  `#4AA8FF` 蓝色；等待、关闭和不可用状态继续共用带斜杠图标，状态逻辑不变。
- 已重新生成包含 16 至 256 像素九种尺寸的 Windows `.ico`；本轮不构建、不打包、不发布。

### 2026-09-08 candidate.25 下午修复与便携测试包

- 本包纳入音量键扩展标记、HID 独立续租、输出端点状态同步、新应用图标，以及按键录入和
  微信语音运行状态修复；两个 Cython 权限模块源码未变，但随完整构建重新生成。
- `build/build-candidate.ps1` 唯一完整入口通过：完整 unittest、公开边界、第三方通知、
  资源释放、Cython 编译态权限测试、PyInstaller、冻结主程序、管理员 HID 助手和 Qt
  运行时检查全部通过。
- 来源提交为 `3fa1ff0`；构建输入 220 个文件，来源指纹 SHA-256 为
  `D65BB0F57E6DA83CD9D33F8D5B449215BF00C9AAEDBD075643140431CFB7464B`；构建产物
  指纹为 `878E221FA8F08B439F4925CCE5B433E46932B6DB03914E8E78433251433414B5`。
- 未签名本机测试包为 `无线麦便携测试包-candidate.25.zip`，大小 163,130,858 字节，
  SHA-256 为 `BABEF2F29B2A56F3830AA5DF0265FD0416A8E530FD25EBB29CC4FF093594B236`；
  包内版本和唯一顶层目录均为 `0.2.0-candidate.25`，共 2126 个文件。真实 RC003、微信
  语音识别与文字上屏、HID 长时间续租和端点状态仍待人工实测。

### 2026-09-08 candidate.24 微信按钮控制与便携测试包 024

- 微信输入法改为唯一使用状态栏语音按钮控制，话筒键按下时打开语音窗口，松开时结束；
  不再录入、校验、模拟或执行微信快捷键。搜狗、自定义程序和 Windows 语音输入保持原路径。
- `build/build-candidate.ps1` 唯一完整入口通过：unittest 2516 项通过、7 项按环境跳过，
  公开边界、第三方通知、资源释放、PyInstaller、冻结主程序、管理员 HID 助手和 Qt 运行时
  检查全部通过。
- 来源提交为 `44a4280`；构建输入 219 个文件，来源指纹 SHA-256 为
  `5B1E30F234038CE882EE5E77BA2E77A8BB0A3CEE8DDD76BCAD230FA7B44E5687`；构建产物
  指纹为 `586FF419180E77DD96DB20808F214E49509A8AC168E3646C5E7ED2A2D8F0FFD5`。
- 未签名本机测试包为 `无线麦便携测试包-024.zip`，包内版本为
  `0.2.0-candidate.24`，只有一个顶层目录、2124 个文件和一个根层主程序；真实 RC003
  按键、微信语音识别与文字上屏、通知区域恢复、旧版接管和长期运行仍待人工实测。

### 2026-09-07 微信输入法直接语音按钮控制

- 本机核实微信输入法 `2.1.3.18` 不响应本程序测试的 Windows 模拟键盘路径，即使无线麦
  与微信同时使用管理员权限也一样；历史成功链路实际使用微信状态栏语音按钮。
- 话筒键按下时直接点击微信语音按钮并确认语音窗口出现，松开时再次点击结束；打开失败
  不发送 `MIC_OPEN`，结束失败保留状态供断连或退出重试，超过 5 秒的残留窗口只在同一
  微信进程内尝试关闭。新会话会废止旧收尾任务。
- 微信快捷键录入、同步、校验和发送全部停用，界面显示“无需设置”；旧配置只作兼容读取。
  搜狗、自定义程序和 Windows 语音输入保持原路径。
- 本机直接控制连续 3 轮打开、结束均成功；受影响范围 133 项运行，132 项通过、1 项按平台
  条件跳过。本轮不构建、不打包，真实 RC003 音频、微信识别和文字上屏待实测。

### 2026-09-07 candidate.23 用户流程与测试包准备

- 语音快捷键录入继续以 Windows 低层键盘钩子为主，设置窗口 Qt 键盘事件为兜底；任一
  通道录入成功后都先完成监听清理，再按当前选择的语音程序校验并保存。
- 搜狗就绪补救绑定本次按住会话；用户快速松手、旧会话结束或新会话已经开始时，旧任务
  不再启动搜狗组件，避免松手后又出现没有对应收尾的语音窗口。
- 设备页和其它设置列表统一使用全局 `0.5` 细分隔线，不再由通用设置单独强制为 1 像素。
- 快捷键录入、输入法校验、按住/松手、搜狗补救和共享设置行共 106 项受影响范围检查通过；
  完整构建入口随后通过 2516 项测试，资源释放、PyInstaller、冻结程序和 Qt 运行时检查通过。
- 构建流程现能识别虚拟环境派生的开发版窗口子进程并正常退出；历史本地测试包、冒烟目录和
  界面审计目录不再被公开边界检查误判为源码。
- 未签名本机测试包为 `无线麦便携测试包-023.zip`，包内版本为
  `0.2.0-candidate.23`，只有一个顶层目录和一个根层主程序；真实 RC003 和输入法流程待实测。

### 2026-09-07 语音快捷键按程序校验

- 语音快捷键保存前按当前选择的语音程序校验：搜狗语音助手 `1.0.1.3272` 沿用其最多
  三个按键、单键和组合键规则；微信输入法 `2.1.3.18` 沿用其修饰键、键数、禁用功能键
  和 Windows 输入法切换组合限制。
- 不合规的录入不再保存，界面恢复原快捷键并直接说明原因；自定义程序继续允许普通单键，
  Windows 语音输入仍固定为 `Win+H`。
- 补全搜狗配置使用的通用修饰键、Win、方向、标点、数字键盘数字与运算键及功能键名称
  转换；当前无法可靠区分的数字键盘 Enter 会明确要求换键，不会假装已支持。相关模块
  25 项、设置保存调用链 3 项定向检查通过；未构建、未打包。
- 本节中的微信快捷键校验属于当时方案，已被上方“微信输入法直接语音按钮控制”取代；
  搜狗、自定义程序和 Windows 语音输入的校验结论继续有效。

### 2026-09-07 实际说话入口、设置行居中与快捷键录入

- 搜狗进程已经运行、语音窗口尚未弹出时，不再阻止“实际说话”测试，也不再误导用户
  退出程序；按下语音键时仍由现有受控流程打开语音窗口。
- 共用设置行改为整行垂直居中，名称、输入框、备注和状态不再因字号不同而视觉偏上；
  删除语音程序区域没有操作作用的“程序启动 / 系统设置”说明行。
- “语音按键”备注统一为“仅支持录入“按住型”快捷键”。录入监听真正就绪前显示
  “正在准备…”，只有就绪后才提示按键；用户明确录入期间可接收远程控制软件转发的
  键盘事件，平时监听仍拒绝注入事件。
- 修正部分 Windows 环境中低层键盘钩子启动成功却收不到按键的问题：保留原精确录入通道，
  并增加应用级 Qt 事件过滤兜底；任一通道完成后都先停止监听再保存，避免重复录入或残留监听。
- 原 24 项定向检查继续有效，新增受影响范围 19 项通过。用户已确认中间兜底实现可以手动
  录入；最终源码仍待重开开发版后人工确认。

### 2026-09-07 candidate.21 搜狗语音窗口与通知区域恢复

- 删除程序启动阶段没有对应按键松开的搜狗组件预热；搜狗程序仍可按设置启动，真正按下
  遥控器语音键时才执行受控就绪检查，并由实体松开或音频停止发送一次快捷键抬起。
- 隐藏窗口后，通知区域图标的普通激活事件统一恢复正常窗口，避免双击图标没有反应。
- `candidate.20` 本机日志已确认蓝牙恢复后方向键可识别，三次实机语音均收到按下、有效音频、
  实体松开和音频停止，搜狗窗口随后不可见。2026-09-07 用户使用 `candidate.21` 实机确认：
  关闭此前遗留的搜狗窗口并重新打开程序后，启动不会再次弹出；遥控器语音键按下可唤起，
  松开可收回。
- 启动门禁、语音按下/松开、迟到重复输入和通知区域源码合同共 38 项定向检查通过。
- 轻量冻结后的主程序 `--dry-run`、Qt 运行时、管理员 HID 助手 `--self-check` 和 ZIP 逐文件
  校验通过；本地测试包为 `无线麦便携测试包-021.zip`，未重跑全项目测试。

### 2026-09-07 candidate.20 三页使用流程与输入生命周期收口

- 迟到的 Windows/F5 重复按键事件不再提前释放当前方向键或语音快捷键；按键松开、断连、
  停止服务和退出仍统一释放，避免双触发、锁键和残留按下状态。
- 快捷键录入先完成键盘监听清理再保存，避免新组合键没有写入或被旧值覆盖；按键检测完成后
  也会明确结束本次检测，再允许进入下一项输入操作。
- 声音通道测试临时停止遥控器服务后，以不启动语音程序的方式恢复服务，避免测试结束后搜狗
  等窗口常驻；实际说话前会检查服务、遥控器连接、管理员按键、话筒键映射、快捷键和音频端点，
  缺一项时直接指出应先完成的操作。
- 新服务启动前清理已停止旧服务留下的运行状态；服务恢复成功后清除过期的恢复按钮和错误提示，
  防止界面继续显示上一轮失败。
- 完整 unittest 2501 项通过、7 项按环境跳过，资源释放门禁通过；PyInstaller 冻结、主程序
  `--dry-run`、管理员 HID 助手 `--self-check`、Qt 运行时和本地 ZIP 完整性检查通过。本地测试包
  为 `无线麦便携测试包-020.zip`，待三页真实使用流程和实体设备回归。

### 2026-09-07 candidate.19 管理员运行权限与首次按键提示

- 管理员 HID 助手升级到第 8 代；注入所需运行文件与助手程序分开保存，只给
  `LocalService` 运行文件读取和执行权限，助手程序本身不放宽权限。发现旧权限时会自动校正。
- 本机运行 `candidate.18` 后，计划任务返回成功，日志进入
  `attached_waiting_for_hid_io`，不再出现 DLL 载入失败，证明权限、注入和 Gadget 连接已越过
  原失败点；真实遥控器按键仍需在有设备的主控端验证。
- 设备页不再把“已挂接、尚未收到首次按键”误报成“请重新连接”。现在显示“请按遥控器”并
  隐藏无效的重连按钮；已经收到过 HID 数据后的真实通道异常仍保留重新连接入口。
- 管理员组件 124 项、设备页状态 3 项定向检查通过；冻结 HID 助手自检、主程序
  `--dry-run`、Qt 运行时与 ZIP 完整性检查通过。本地测试包为
  `无线麦便携测试包-019.zip`，待真实遥控器按键、语音快捷键和后续普通启动复测。

### 2026-09-06 candidate.17 管理员按键助手升级与状态文案

- 管理员按键助手升级到第 7 代，避免 `candidate.16` 再次复用较早构建留下的第 6 代
  助手。首次确认 UAC 后由固定计划任务按需运行，桌面主程序仍保持普通权限。
- 助手失败不再统一返回模糊的 `4`，现在可区分助手身份、HID 系统宿主、调试权限、
  目标进程、运行文件、远程内存、远程线程和 DLL 载入阶段，便于从日志直接定位。
- “按键接收”和“遥控器服务”保留一行功能说明，异常状态改为明确操作；状态与说明
  固定完整显示，通用设置分隔线恢复可见。
- 更新检查遇到较新但未上传完整的版本时直接提示，不再回退推荐旧包；程序只清理自己
  创建的当前或过期下载缓存，陌生文件、链接和更高版本目录保留。
- 权限和注入相关 174 项测试通过，设备页文案与状态 3 项定向检查通过；冻结程序的
  HID 助手自检、主程序 `--dry-run` 和 Qt 运行时检查通过。本地测试包为
  `无线麦便携测试包-017.zip`；普通权限实机链路待该包验证。

### 2026-09-06 candidate.16 手动检查更新与旧服务接管

- “设备”页“运行日志”旁新增手动“检查更新”。只有用户点击后才访问固定的
  `ZSTDJan/windows-remote-mic-app` GitHub Releases；启动和后台运行不自动联网，
  稳定版不会误用预发行通道，版本比较读取发布资产中的内部版本而不是仓库 tag。
- 安装版下载同次发布的安装器，便携版或源码运行下载便携 ZIP。程序先校验资产大小、
  `SHA256SUMS.txt` 及 GitHub 提供时的 SHA-256 摘要，使用 `.part` 临时文件并支持取消；
  下载完成后只打开文件夹，不自动安装、覆盖或运行下载文件。
- 补齐网络读取超时、限流、未完成资产、退出竞态和文件被移动后的恢复：可逆退出失败时
  不会把更新状态永久卡在忙碌中，已下载文件不存在时会重新出现“重新下载”。
- 桌面窗口创建前先安全接管历史独立遥控器服务；旧版重新抢占、退出失败或状态清理无法
  确认时停止打开当前版本，不带着两个服务继续运行。
- GitHub 首页最前方明确提示当前公开版暂时以管理员身份启动，同时保留普通权限主程序配合
  按需管理员助手的目标架构。本批受影响范围 312 项自动检查通过；未构建、未打包、未发布。

### 2026-09-06 candidate.15 管理员按键、旧服务接管与测试包 015

- 继续修正真实 Windows 计划任务差异：允许任务计划程序省略值等于系统默认值的设置节点，
  并在等待管理员注入任务结束时主动刷新运行实例，避免组件已经完成却仍被旧状态误判为
  运行中或安装无效。当前账号不能自行提权时不再提供点了也办不到的按钮，直接提示更换
  管理员账号；任务动作、路径、权限主体、助手哈希和无触发器边界不变。
- 桌面程序启动前先处理历史独立遥控器服务。旧服务能正常退出时，清除属于该旧进程的运行
  状态后再启动当前进程内服务；退出或状态清理无法确认时停止打开新版并给出可执行提示，
  不带着旧状态进入界面。运行状态文件按 Windows 登录会话隔离，清理时先原子认领再核对
  PID，避免并发登录会话或新旧进程交接时互删状态。
- 设备页按真实设备、Raw Input、HID tap 和语音按键通道分别判断状态；需要用户操作时，
  右侧按钮直接切换为“立即连接”或“重新启动”。状态和备注改为短句并固定完整显示，
  不再以省略号隐藏关键提示；新配置点击窗口关闭按钮仍默认隐藏到通知区域。
- 最终完整 unittest 2430 项通过、7 项按环境跳过；PyInstaller、冻结主程序、HID 助手、
  Qt 运行时和 ZIP 完整性检查通过。本地测试包为 `无线麦便携测试包-015.zip`，仅用于
  本机实测。最终复核发现旧版可在迁移检查与当前服务真正启动之间再次抢占，`015` 因此
  已由 `016` 取代，不再继续测试；`012` 同样保留为历史记录。

### 2026-09-06 candidate.12 管理员按键任务兼容与测试包 012

- 根据 `011` 的本机失败继续核实，确认 Windows 可能把计划任务权限规范化为等价但不同的
  SDDL：SYSTEM 和管理员权限会被拆分或标成继承，旧校验因要求固定文本形态而误判无效并
  回滚。当前按实际有效权限合并校验，仍只允许 SYSTEM、管理员和当前用户；当前用户只能
  读取和运行任务，额外主体、额外权限、真实触发器或额外动作仍全部拒绝。
- 安装后优先校验注册调用直接返回的任务；只有该对象暂时不可读时才重新查询。管理员助手
  和普通主程序都只对“任务暂不可见/暂不可查询”做最长 5 秒的有界重查，真实 XML、权限、
  路径、清单或回滚错误立即失败，不会被重试掩盖。
- 管理员助手为安装、复制、权限、任务、清单和回滚阶段返回可区分的退出结果，普通主程序
  写入脱敏失败阶段，界面仍只给出简短重试和查看日志提示；同时补齐清单写入后复核、复制
  失败归类和安装回滚边界。
- 受影响范围 745 项测试通过且无资源泄漏告警；最终公开边界扫描 435 个文件，完整
  unittest 2398 项通过、7 项按环境跳过；PyInstaller、冻结主程序 `--dry-run`、HID 助手
  `--self-check`、Qt 运行时和 ZIP 完整性检查全部通过。`011` 已由 `012` 取代，真实首次
  UAC、重新登录自启、自定义映射、搜狗语音、实体方向键和旧版接管仍为检查点待实测。

### 2026-09-06 candidate.11 管理员按键权限、关闭行为与测试包 011

- 修正 `010` 首次确认 UAC 后管理员按键组件仍启用失败的问题：Windows 会为按需计划
  任务补出空 `Triggers` 容器，旧校验误判为自动触发器并回滚。当前只接受唯一、直属、
  无属性且无内容的空容器，真实触发器、额外动作、自动维护和失败重启仍全部拒绝。
- 主程序和登录自启继续使用普通权限；当前账号首次打开固定管理员按键权限时确认一次
  UAC，成功后普通启动、重复启动和登录自启不再反复询问。较新助手不会被旧版覆盖或
  删除，管理员助手不写用户目录，主程序只落一条脱敏结果日志。
- 首次弹窗简化为“建议打开管理员按键权限”，高亮“打开”，并提供文字式“不打开”。
  新配置点窗口 `×` 默认隐藏到通知区域；已有合法选择继续保留，选择“完全退出”时仍
  完整停止全部长期资源。
- 按键检测成功测试改为等待真实异步停止完成，并确认监听器只停止一次；补齐管理员助手
  同一宿主最多三次瞬时重试、第四轮停止、新宿主重置次数和失败后恢复成功的边界测试。
- 最终公开边界扫描 435 个文件；完整 unittest 2376 项通过、7 项按环境跳过；资源泄漏
  门禁、PyInstaller、冻结主程序 `--dry-run`、HID 助手 `--self-check` 和 Qt 运行时检查
  全部通过。未签名本机测试包 `无线麦便携测试包-011.zip` 保留为历史构建记录；
  2026-09-06 已由 `012` 取代，禁止继续测试，也不能作为当前候选或发布依据。

### 2026-09-05 candidate.10 权限口径收口与本地测试包 010

- 首次授权、管理员启动拦截、设置页、安装器和便携说明统一说明：管理员按键组件负责
  全部自定义按键映射；组件不可用时全部自定义映射停用，只保留 Windows 原始按键操作。
  本轮没有改变 candidate.9 已完成的按键执行、单实例、退出和升级逻辑。
- 最终公开边界扫描 435 个文件；完整 unittest 2351 项通过、7 项按环境跳过；
  PyInstaller、冻结主程序 `--dry-run`、HID 助手 `--self-check`、Qt 运行时、
  Inno Setup 6.7.3 编译和 ZIP 逐文件哈希回读全部通过。
- `无线麦便携测试包-010.zip` 保留历史构建记录；当前已由 `011` 取代，禁止继续测试，
  也不能作为当前候选或发布依据。

### 2026-09-05 candidate.9 全范围收口与本地测试包 009

- 管理员按键组件不可用时，全部自定义按键映射统一停用，只保留 Windows 原始按键
  操作；组件恢复后先等待真实松开再重新接管，避免半个手势、重复触发或旧长按状态
  串入下一轮。
- Raw Input 启动、退出回调、监听代次切换和恢复流程统一串行；已经退出或属于旧代次的
  listener 不能再被误报为就绪，也不能修改当前按键状态。
- 主程序和登录自启继续保持普通权限；当前账号首次启用固定 HID 助手时确认一次 UAC，
  此后普通启动和登录自启不再反复授权。单实例、跨版本接管、默认完整退出及安装升级
  继续走正常清理路径，不允许多个长期主程序并存。
- 安装器只在新版文件验证和旧版备份提交都成功后删除历史快捷方式；验证、提交或退出
  失败时保留旧版和原快捷方式。构建来源指纹补齐两份 README、停止脚本及交付合同。
- 最终公开边界扫描 435 个文件；完整 unittest 2351 项通过、7 项按环境跳过，资源释放
  复测、PyInstaller、冻结主程序 `--dry-run`、HID 助手 `--self-check`、Qt 运行时、
  Inno Setup 编译和 ZIP 逐文件哈希回读全部通过。
- `无线麦便携测试包-009.zip` 保留历史构建记录；当前已由 `010` 取代，禁止继续测试，
  也不能作为当前候选或发布依据。

### 2026-09-05 HID 助手与便携版接管收口

- 修正冻结 HID 助手遗漏 `VERSION` 导致首次启用方向改键时弹英文崩溃框的问题；助手
  导入或运行异常改为静默失败码，构建和打包前必须实际运行 `--self-check`。
- 当前版本在真实 QML 窗口和完整退出信号就绪后立即发布接管能力，不再等待可选元素
  导航启动；跨版本启动会给能力发布留出 10 秒，不会在正常冷启动阶段误判为旧版。
- 旧窗口取消未保存修改、保存失败或正常退出失败时，会把结果回传给等待中的新版本并
  立即停止切换；无法回传结果的较早版本也只保留 45 秒有界等待。
- 本机构建与打包锁扩大到整台电脑，安装脚本、随包说明和法律文件纳入同一来源指纹；
  内部构建版本升为 `0.2.0-candidate.8`，旧测试包 007 作废。
- 最终公开边界扫描 520 个文件；完整自动测试 2156 项通过、7 项按平台或安全条件跳过；
  PyInstaller、冻结主程序、冻结 HID 助手自检和 Qt 运行时检查全部通过。
- 新的未签名本机测试包为 `无线麦便携测试包-008.zip`，包内只有一个顶层目录，根层
  只有主程序 EXE，HID 助手、`VERSION` 和来源指纹均位于 `_internal`；真实旧版接管、
  首次授权、自启动、方向键和完整退出仍为检查点待实测。

### 2026-09-04 断线等待时支持立即重连

- 遥控器离开蓝牙范围后，服务正常运行但尚未重新连接时，设备页原“重新启动”按钮改为
  “立即重连”；点击后跳过当前最长 60 秒的退避等待，直接开始下一次连接尝试。
- 该操作不停止或重启服务，也不会创建并发连接；连接中会暂时禁用重复点击，连接成功后
  立即恢复正常状态。旧版服务、状态过期或按键通道异常仍保留“重新启动”。
- 受影响的连接监督器、桥接生命周期、应用接线、Qt 控制器和真实离屏 QML 共 469 项
  测试通过，1 项按平台条件跳过；本轮不构建、不打包、不发布，真实 RC003 仍待复测。

### 2026-09-04 鼠标动作进入统一按键映射

- 单击、双击、长按和遥控器组合的统一动作列表新增鼠标左键、右键、中键、X1、X2
  单击，以及滚轮向上、向下；动作只操作当前鼠标位置，不增加移动、拖拽或单独按下。
- 鼠标点击以同一 `SendInput` 批次发送按下和抬起。部分发送或提交异常后会立即补发
  mouse-up；仍不能确认时保留具体鼠标键，并在下一动作、重连和退出前重试，完成前
  禁止继续注入。
- 实现前按 clean-room 方法只读调查了 Quicker 官方资料和本机签名程序集，采用注入
  状态分离、按下/抬起所有权一致性原则，不复制第三方方法正文、界面或资产。
- 精确暂存候选共运行 553 项，552 项通过、1 项按平台条件跳过；其中已包含真实离屏
  QML 直接保存集成检查，`py_compile` 通过。
- 本轮不升级配置 schema，不构建、不打包、不发布；旧版程序无法识别新增动作，降级
  前需先改回旧版支持的映射。真实 RC003、鼠标目标、X1/X2 和连续滚轮仍待人工实测。

### 2026-09-04 鼠标映射板块复核与输入所有权加固

- 修正设置热加载与普通按键计时器并发时，旧单击/双击/长按/连发或组合手势可能读取
  新映射的问题；活动手势和已经产生但尚未执行的回调现在共同持有保存前映射，结束后
  才应用新配置。
- 主映射发送左、右、中、X1、X2 点击前避让实体同名按键。元素导航在移动指针前检查
  五种实体鼠标键；正在拖拽时不再先移动指针再报告失败。
- 元素导航点击改为成对批量 `SendInput` 并检查送达数量；补松仍未确认时保留具体鼠标键，
  后续点击、滚轮、退出导航和进程停止都会先重试，完成前不再继续鼠标操作。
- 最终按键映射、设置界面、输入接线、Win32 ABI、元素导航及独立导出共运行 1074 项，
  其中 1073 项通过、1 项按平台条件跳过；`py_compile` 和差异检查通过。
- 本轮不构建、不打包、不发布；真实 RC003、实体鼠标拖拽、X1/X2、滚轮和异常退出后的
  输入状态仍为检查点待实测。

### 2026-09-04 便携版切换提示简化并避免静默等待

- 首次确认只保留“旧版正在运行、是否退出旧版并打开当前版本”；旧版不能自动退出及
  等待期间再次双击时，只提示去通知区域选择“完全退出”，不再堆叠内部判断与重复说明。
- 修正旧窗口唤出信号发送成功后，程序就误以为用户已经得到操作指引的问题。旧版 5 秒
  内没有接收退出请求时，无论窗口信号是否发送成功，都会立即提示从右下角通知区域右键
  “无线麦”并选择“完全退出”；旧版退出后当前版本继续自动打开。
- 保留单一等待者、正常退出、180 秒最终超时和请求清理边界，不强制结束旧版。
- 内部构建版本升为 `0.2.0-candidate.6`，避免与可能静默等待的 `.5` 测试包混淆。

### 2026-09-04 便携版双击安全切换到当前版本

- 修正不同目录或旧版本已经运行时，双击新便携版只会唤出旧窗口并让新进程退出的问题。
  每个“版本加所在目录”现在持有独立运行身份；同一副本重复双击仍只恢复原窗口，点击
  其它副本则明确询问是否切换。
- 用户确认后，支持完整退出请求的旧版会处理未保存提示并正常退出；更早的旧版会被唤到
  前台，用户从通知区域选择“完全退出”后，同一次双击会继续打开当前版本。整个过程不
  强杀旧进程，不丢弃未保存映射。
- 每个登录会话只允许一个版本切换等待者；退出请求带本次切换的唯一编号，只清理自己
  写入的请求。旧版退出、请求清理和产品级单实例重新取得运行权全部成功后，新版才启动。
- 同一新版正在等待旧版退出时再次双击，只会精确查找该版本、该目录自己的窗口；找不到
  时会明确说明当前仍显示的可能是旧版。支持自动退出的旧版一旦接收请求，新版会安静
  等待后台资源清理，不再弹出错误的手动退出提示。
- 最终完整 unittest 1954 项通过，7 项按平台或安全条件跳过；公开边界扫描 510 个文件、
  PyInstaller 主程序与 HID 助手构建、冻结 `--dry-run` 和 Qt 运行时检查通过。
- 本次行为变更升为 `0.2.0-candidate.4`，避免与未包含便携接管的 `.3` 构建混淆。

### 2026-09-04 覆盖安装可识别新旧版本并安全接管

- 程序、PyInstaller、安装器和 CI 改用同一份 `VERSION`，当前窗口标题显示
  `0.2.0-candidate.4`，避免旧安装版版本号高于新安装包时无法判断实际启动的是哪版。
- 安装完成页默认打开刚安装的新版本，但不会自动启动遥控器服务。旧版尚在运行时，
  安装器先正常停止旧遥控器服务；旧设置窗口会被唤到前台并等待用户完全退出，不再为
  方便覆盖而强杀并冒险丢失未保存映射。
- 首次安装即使目标目录还不存在，也会检查其它目录中正在运行的同名程序。发现旧便携版
  时会唤出旧窗口并停止安装；用户完全退出旧版后重试，安装完成页才能确定打开新版本。
- 新安装和旧配置升级后，关闭窗口默认会完整退出；用户主动选择“隐藏到通知区域”后仍
  可保留后台运行。该迁移解决任务栏“关闭窗口”实际只是隐藏、看起来没有反应的问题。

### 2026-09-04 成品目录只保留一个用户入口

- 新构建的根目录只保留 `RemoteMicRC003.exe`。独立管理员 HID 助手仍保留原有权限
  隔离和窄职责，但移到 `_internal`，只由安装器、“修复权限”和固定计划任务自动调用，
  用户不需要也不应手动打开。
- 覆盖安装会精确删除旧版遗留在安装目录根层的助手 EXE；本地构建和 CI 同时检查根层
  只能有一个 EXE，避免再次把内部组件暴露成第二个程序入口。
- 相关定向测试 172 项、最终完整 unittest 1954 项通过，7 项按平台或安全条件跳过；
  公开边界、冻结运行和 Qt 运行时检查通过，安装器已重编。覆盖安装、首次授权、修复
  权限和登录自启仍为检查点待实测。

### 2026-09-04 安装升级前安全退出旧版

- 覆盖安装和卸载前不再按模糊进程名强杀。当前版本通过一次性文件请求同一个桌面主
  进程完成正常退出；有未保存的按键修改时会恢复窗口，让用户选择保存、不保存或取消。
- 退出请求处理期间禁止新的桥接启动、自动恢复和手动重启。当前版本拒绝退出或超时、
  旧版控制入口缺失、进程身份无法确认时，安装器停止覆盖并给出可以直接处理的提示。
- 已发布的旧双进程版本先正常停止桥接并确认其退出，再唤出仍在运行的设置窗口，等待
  用户保存或放弃未保存修改并选择“完全退出”；旧托盘控制窗口必须属于目标桥接 PID，
  避免误发退出命令。
- 只有明确权限不足才为关闭旧管理员进程额外请求一次 UAC；控制窗口不存在不会触发
  无用提权。升级确认旧进程全部结束后会重写纯程序 `_internal` 目录，但保留用户设置、
  日志和采集数据。
- 最终完整 unittest 共 1920 项通过、7 项按平台或安全条件跳过，且未出现
  `ResourceWarning`；本轮不构建、不打包、不发布，真实旧版覆盖安装、管理员旧版、
  UAC 取消、未保存提示、配置日志保留和卸载流程仍为检查点待实测。

### 2026-09-03 方向键单次触发、单进程生命周期与预授权 HID 助手

- 修正同一次小米遥控器2 Pro 方向键同时经过 Windows 原始输入和无线麦映射注入，导致
  前台程序移动两次的问题。上下左右现在只由更早到达的 HID tap 执行自定义映射；
  HID tap 不可用时不再从迟到的 Raw Input 补做映射，只保留 Windows 原始方向行为。
- HID tap 从可用状态失联时，会在同一输入仲裁锁内先补齐全部已认领按键的松键，再
  切换到 Raw Input 降级，避免最后一次方向或映射按下没有对应抬起而持续连发。
- 正常产品改为每个 Windows 登录会话只保留一个长期 `RemoteMicRC003.exe`。窗口、通知
  区域、桥接 worker 和元素导航都由该主进程持有；重复启动只恢复已有窗口，兼容
  `--bridge` 只通知现有进程启动服务。升级时可控制的旧独立桥接先正常退出再迁移；
  无法安全控制的更老实例明确提示用户手动退出，不猜测 PID 强杀。
- 元素导航正常路径已嵌入桌面 Qt 事件循环，不再创建长期伴随进程；隐藏的独立入口只作
  旧版本兼容。右 Alt 语音物理化钩子改用有效 `MSG` 缓冲创建消息队列，私有注入标记
  统一由 `voice_key_physicalizer_windows.py` 定义。
- 主程序和登录自启继续使用普通权限。安装完成后单独确认一次 UAC，把不接受
  PID、程序路径或用户配置的独立 HID 助手复制到 Program Files，并登记一个无登录
  触发器、只运行固定 `--inject` 动作的按需计划任务；日常启动和登录自启不再弹 UAC。
- “设备”页会明确显示管理员按键组件异常，并提供“修复权限”。组件缺失、被替换或任务
  定义不符合固定合同时，自定义方向映射保持停用；修复成功后会正常重启运行中的桥接。
- 当前预授权方式要求正在登录的 Windows 账号本身属于管理员组；标准账号在 UAC 中输入
  另一管理员账号凭据，不能获得当前账号可无提示调用的管理员任务。最终完整 unittest
  共 1889 项通过、7 项按平台或安全条件跳过，且日志未出现资源泄漏告警；`compileall`、
  源码 `--dry-run`、公开边界扫描 507 个文件、`pip check` 和 `git diff --check` 通过。
  本轮不构建、不打包、不发布，安装、重登自启、实体方向键、助手恢复和卸载仍为检查点
  待实测。

### 2026-08-31 开源发布基础与许可门禁

- 新增安全报告、贡献说明、行为准则、Issue/PR 模板和 Dependabot 配置，明确 Windows
  RC003 的问题范围、隐私边界、真机证据和贡献许可。
- 补齐当前冻结依赖的第三方许可目录与精确清单；候选构建新增依赖声明检查，安装版和
  便携版都会携带许可、对应源码和素材授权记录。
- PyInstaller 明确排除未使用的 ASIO PortAudio DLL，收窄没有产品收益的第三方许可面。
- Windows CI 固定 Windows/Python/Actions/Inno Setup 入口，增加手动运行、main/tag
  范围、并发和总超时；只有正式 tag 通过发布门禁后才上传分发 artifact，不自动创建
  GitHub Release。
- 当前照片公开再分发授权和 Qt/PySide6 项目控制的对应源码地址仍未确认，正式 tag 会
  被 `check-release-readiness.py --enforce` 阻断。本轮不改内部版本号、不构建、不
  打包、不发布；现有功能改动冻结后再统一切换到 `0.2.0-candidate.1`。

### 2026-08-31 服务自检、语音结果诊断与首次唤起修复

- 桥接运行状态升级为兼容旧文件的 schema 2，增加版本/构建来源、BLE、Raw Input、
  HID tap、最近按键和语音活动，并以 5 秒心跳保持新鲜度。设置页不再只凭互斥锁显示
  “已运行”；其它构建或两个按键通道明确失败时仅在语音空闲状态正常恢复一次，旧状态
  或心跳滞后只提供人工“重新启动”。
- 语音开始和结束增加隐私受限诊断，只比较前台进程、控件类名、焦点和文本长度，明确
  区分“面板关闭”与“文字已上屏”，不读取窗口标题、文字或语音内容。
- 搜狗通过已登记的 `SogouComMgr.exe` 组件入口在桥接启动时预热一次；实际按键后若可见
  语音窗口仍未就绪，只再修复检查一次，不循环发送快捷键。微信复用同一结果诊断，
  不套用搜狗预热。
- “显示桌面”由切换型 `Win+D` 改为单向 `Win+M`，遥控器重复上报不会再把窗口恢复；
  其它内置动作保持现有行为。完全退出继续等待桥接正常清理，不新增强杀路径。
- 自动检查完成后仍需用真实 RC003、搜狗/微信输入法和目标输入框验证首次唤起、文字上屏、
  显示桌面连按和完全退出；本轮不构建、不打包、不发布。

### 2026-08-30 对外显示名统一为“无线麦”

- 窗口标题、通知区域、用户提示、安装器、开始菜单、桌面快捷方式和公开使用说明统一
  显示“无线麦”。升级安装会删除本项目旧版创建的精确旧名称快捷方式，避免两套入口并存。
- `RemoteMicRC003.exe`、安装包文件名、`%LOCALAPPDATA%\RemoteMic\RC003`、自动
  启动键、Mutex 和内部模块名保持不变，现有安装与用户配置无需迁移。

### 2026-08-30 统一桌面程序生命周期与三态图标

- 在“设备”页“运行日志”下方新增独立“通用设置”分组：三行不显示左侧装饰图标，
  可明确开启随 Windows 启动、让程序打开后自动执行一次“启动桥接”，并选择关闭窗口时
  隐藏到通知区域或完全退出。三个选项互不绑定；默认隐藏，完全退出会先正常停止桥接，
  失败时恢复窗口并提示。
- Qt 桌面程序统一持有任务栏窗口和通知区域图标；由设置程序启动的桥接子进程不再显示
  第二个图标。服务未启动、等待小米遥控器2 Pro、小米遥控器2 Pro 已连接分别显示
  灰色斜杠麦克风、灰色麦克风和蓝色麦克风，窗口与任务栏图标同步更新。
- 配置 schema 升到 9；登录自启只写当前用户 `Run` 项，源码模式使用绝对
  `src/launcher.py` 入口，冻结版使用当前 EXE。安装器不替用户启用自启，卸载时只删除
  本程序拥有的启动项。
- 修复从通知区域选择“完全退出”后设置进程仍可能驻留的问题：退出确认后，窗口关闭
  事件会放行真正退出，不再被“关闭时隐藏到通知区域”规则再次拦住；未运行桥接时立即
  退出，桥接运行时仍先正常停止服务，异常时恢复可重试状态并显示错误。
- 生命周期、配置、入口、Qt、安装器、资源、隐私与公开边界相关定向测试 700 项通过，
  2 项按平台条件跳过；`compileall`、公开边界扫描 324 个文件和 diff 检查通过。本轮
  不构建、不打包；真实任务栏关联、关闭隐藏与恢复、登录自启、自动桥接和三态图标
  仍为检查点待实测。

### 2026-08-30 修正语音长按后的跨轮按键残留

- 修正音频已经停止、Raw Input 话筒 down 却迟到时被误认成下一轮的问题；迟到边沿
  现在并回原手势，不会额外发送一个没有匹配抬起的宿主快捷键按下。
- legacy F5 继续只负责阻止原生 F5 泄漏，并辅助已有手势收尾；它不会启动、发送或
  释放宿主语音快捷键。迟到 F5 up 也不能串入下一轮、普通映射或按键检测。
- 完整 unittest 1633 项通过、7 项按平台条件跳过，公开边界、语法、依赖和 diff
  检查通过。本轮不构建、不打包；真实 RC003 长按后立即键入和操作鼠标仍为检查点
  待实测。

### 2026-08-29 BLE 控制与阻塞音频播放隔离

- 解码后 PCM 改为快速进入 64 帧有界 FIFO，PortAudio 阻塞写入由独立播放
  worker 串行执行，不再占用 BLE 解码与普通控制事件回调。
- `AUDIO_STOP` 等待 FIFO 屏障后再执行宿主收尾；队列满、写失败、屏障超时
  都停止转发并请求重连。清理保持 worker 先停、sink 后关的所有权顺序。
- 完整 unittest 1651 项通过、7 项按平台或安全条件跳过。本轮不构建、
  不打包；真实 RC003 短说、长说、连续两次、断开/重连和休眠恢复为检查点待实测。

### 2026-08-29 恢复系统字体打包方案

- 当前开发分支重新统一使用 Windows 系统中文 UI 字体，删除三套 Noto Sans SC
  字体及加载入口，冻结目录和本地测试 ZIP 均不再包含字体文件。
- 系统字体使按键编辑提示框边界变化后，共享提示框按自身边框增加避让距离，真实
  QML 渲染检查确认不再压住输入框。完整构建门禁 1599 项通过、7 项按平台条件跳过，
  PyInstaller 冻结与 EXE 自检通过；真实 RC003 和宿主输入法仍待人工实测。

### 2026-08-29 收窄微信快捷键同步并修正设置页状态

- 微信输入法不再通过 UI Automation 自动读取或修改快捷键，避免打开设置窗口、控制
  鼠标和依赖易变的界面结构。Remote Mic 只按程序记忆微信的“按住说话”快捷键，用户
  可从同一行的“打开设置”进入微信设置并手动保持一致；搜狗仍保留静默读取、原子同步
  和读回确认，Windows 语音输入仍固定为 `Win+H`。
- 搜狗、微信输入法和 Windows 语音输入统一在“语音按键”行提供设置入口；搜狗改为
  通过自身托盘菜单打开独立语音设置窗口，全程不移动鼠标。未安装搜狗语音时按钮显示
  “去安装”，能找到 AI 工具箱就打开并在底部状态栏提示手动安装，找不到时只提示；
  微信调用 `wetype_update.exe -showsetting`，Windows 打开系统语音设置。自定义程序
  不显示该按钮。
- 组合快捷键编辑器初始化不再回写控制器，首次打开设置页不会误报“设置已修改”；桥接
  从未连接变为已连接时会排队重新检查，更新“按键接收”等启动前旧结果，且不会启动
  重叠诊断线程。

### 2026-08-28 语音程序快捷键自动读取与同步

- 配置 schema 升到 7，搜狗、微信输入法、Windows 语音输入和自定义程序分别记忆
  “按住说话”快捷键；旧全局值迁移给原来选中的程序，其它程序使用各自原生或固定
  默认值。
- 切换搜狗或微信输入法时优先读取真实快捷键。搜狗读取并原子修改自身 JSON 配置，
  运行中拒绝改写；微信输入法通过自身设置窗口的 UI Automation 语义控件读取和修改，
  不写私有 MMKV，也不使用 OCR、固定坐标或进程注入。Windows 语音输入固定为
  `Win+H`，自定义程序只保存本地值。
- 录入新快捷键后先同步第三方并读回，成功才保存 Remote Mic；同步、保存或回滚失败
  都恢复界面原值并明确提示。完整 unittest 1568 项通过、7 项按平台条件跳过；
  公开边界扫描 361 个文件，`compileall`、`pip check` 和文档合同通过。本轮不构建、
  不打包，真实 RC003、搜狗/微信改写、
  声音接收和文字上屏仍为检查点待实测。

### 2026-08-28 RC003 右 Alt 释放所有权修复

- 防锁日志确认 22 轮 RC003 右 Alt 异常包含 27 条带 `RMICRC03` 标记的 DOWN、
  0 条同标记 UP；19 轮后来由外部程序解除。该私有标记由本程序传给
  `keybd_event`，本次直接来源不是 UU。
- 物理 F5 变换成功注入 DOWN 时现在立即登记匹配 UP 的所有权；即使排队业务回调
  因断连、模式切换或退出变成过期，cleanup 仍会补发一次对应 UP。清理开始后关闭
  本代输入入口，防止在最终释放之后又落入一个迟到 DOWN。
- 兜底只处理本程序明确登记的注入，不对系统 Alt 状态做猜测性解锁。相关 213 项
  通过、1 项跳过；完整 unittest 1536 项通过、7 项跳过。本轮未构建或打包，真实
  RC003 和宿主输入法仍待人工复测。

### 2026-08-28 微信输入法专用开关协议

- 实体交互仍是按住话筒键说话、松手结束，设置和配置继续保持 HOLD-only，不恢复
  遥控器点按持续语音。
- 选择微信输入法时，实体按下被翻译为一次无标记虚拟键短点；物理松手不会提前停止
  尾音，等 RC003 音频停止后再短点一次结束。搜狗、Windows 语音输入和自定义程序
  继续使用原有按下、抬起协议。
- 微信两次短点、其它程序保持按住、尾音延后和异常安全释放已有自动测试覆盖；完整
  构建门禁 1523 项通过、7 项按平台条件跳过，公开边界、PyInstaller 与冻结 Qt 自检
  通过。真实 RC003、微信输入法唤起、声音接收和文字上屏仍为检查点待实测。

### 2026-08-28 微信输入法控制时序修正

- 上一测试包把微信快捷键的按下和抬起放在同一次零间隔 `SendInput` 中，真机反馈
  仍无法唤起微信。当前实现改为先尝试状态栏按钮，再使用分开的快捷键按下和抬起
  批次作为兜底。
- 微信现在先点击 `wetype.statusbar.window` 状态栏语音按钮并确认“语音输入”面板；
  失败时才用分开的虚拟键 `SendInput` 按下/抬起批次，保持 80 ms，并在音频停止后
  按本轮实际成功的状态栏或快捷键路径结束。搜狗等程序继续使用原按下/抬起协议。
- 微信状态栏、快捷键兜底、80 ms 时序、音频停止后结束和非微信程序隔离的定向测试
  已通过；真实 RC003、微信唤起、声音接收、文字上屏及搜狗回归仍待测试包实测。

### 2026-08-28 微信提交残留异步收尾

- 真机日志确认微信与搜狗已经分开走各自协议，搜狗正常；微信能够完成语音识别，
  标准可编辑输入框可直接上屏，远程窗口会由微信提示“结果已复制，粘贴即可使用”。
- 微信结束触发后不再阻塞遥控器输入链路，后台最多等待 5 秒让微信自行完成；面板仍
  残留时才发送 `WM_CLOSE`。新会话或桥接清理会使旧收尾失效，避免误关下一轮面板。
- 本程序不读取剪贴板、不自动粘贴，也不改变搜狗协议。自动检查通过，残留面板是否
  按预期消失仍需当前源码真机复测。

### 2026-08-28 语音页独立保存与麦克风权限收敛

- “应用”移到“安装虚拟音频”右侧，只负责重新检测 VB-CABLE，并自动选择、预检、
  保存可用的 `CABLE Input`；输出端点下拉改为选择后立即保存。
- 语音程序、程序路径、管理员启动和语音按键改为独立自动保存，不再调用按键页的
  总保存，也不会带入尚未保存的按键映射。
- 删除当前语音页“声音输入”入口和设默认麦克风相关引导，只保留“麦克风隐私”
  打开 Windows 权限页；程序不修改 Windows 默认麦克风。
- 麦克风权限不再显示无法由系统可靠确认的固定“待确认”；语音程序和语音按键
  明确显示保存状态，并由真实 QML 事件验证语音按键录入后已经写入配置。
- 底部成功、错误和未保存提示按设备、按键、语音三页归属显示，切换页面不再把
  上一页提示带入当前页；按键映射未保存只在按键页显示。
- 本次补充的自动保存、失败回退、真实 QML 和四组紧凑布局定向检查通过；未构建
  或打包，仍需用户在开发版中确认实际显示与操作。
- Qt 设置模块 190 项和语音程序 QML 专项 1 项全部通过；Basic、FluentWinUI3
  紧凑视口渲染无越界或重叠。本轮未构建或打包，状态为检查点待实测。

### 2026-08-27 按键卡片与语音页列对齐

- 按键映射卡片高度由 `55px` 调整为 `49px`，页面改为直接使用卡片的唯一高度来源；
  默认和最小窗口下，顶部视图切换、13 张卡片和底部操作栏保持分离。
- 语音页状态统一放到最右按钮左侧，状态列固定为 `54px`，最右按钮列固定为
  `120px`；下拉框、路径和快捷键输入从左侧编辑区开始并占用剩余宽度。
- Qt 设置模块 185 项通过；Basic、FluentWinUI3 在 `640x480`、`720x500` 的真实
  QML 检查无越界或重叠。本轮未构建或打包，状态为检查点待实测。

### 2026-08-27 Quicker URI 与遥控器组合映射

- 单键、双击、长按和遥控器组合的普通动作输入支持
  `quicker:runaction:动作ID或名称?参数`，只调用 Windows URI 处理器，不开放任意
  URI 或 shell 命令。
- 按键页新增“单键与手势 / 遥控器组合”顶部通长切换，原检测、恢复和保存操作栏
  固定到底部。组合只允许 TV、菜单或主页作为主键，第二键限方向、确定、返回和
  音量加减；主键双击/长按、话筒、电源及主页+菜单配对组合均排除。
- 组合命中后抑制两个按键的单键动作，未命中时保留主键单击；一次按住主键可依次
  触发多个第二键。配置 schema 升到 6，旧配置兼容，冲突或损坏组合失败关闭。
- 受影响范围 452 项运行，451 项通过、1 项按平台条件跳过；两套 Qt 样式和最小窗口
  的实际渲染检查通过。本轮未构建或打包，真实 RC003 与 Quicker 权限组合待实测。

### 2026-08-27 DJI 入口收窄与微信输入法自动识别

- “当前设备”只保留 RC003，自动诊断不再运行 DJI Mic 2 录音端点检查。DJI 的
  设备档案、端点识别函数和旧配置兼容分支继续保留；旧配置仅打开设置时按 RC003
  显示，不会自动改写磁盘。
- “语音程序”新增微信输入法。程序会从运行进程、卸载注册信息、开始菜单和标准
  安装目录自动识别，新电脑正常安装后无需用户寻找路径。微信输入法由 Windows
  管理，不随桥接强行启动，也不显示管理员启动选项。
- 受影响定向测试共 309 项运行，307 项通过、2 项按平台条件跳过；Qt 设置模块
  167 项全部通过，真实 QML 探针确认 DJI 不可选择、RC003 页面正常显示。本机
  只读检查已识别微信输入法安装目录和运行进程。本轮未构建或打包，状态为检查点
  待实测。

### 2026-08-27 设置界面改为设备、按键、语音三页

- 设置窗口改为“设备 / 按键 / 语音”三页。设备页集中 RC003、按键接收、遥控器
  服务和运行日志；按键页保留 13 键实物映射；语音页集中虚拟音频、输出端点、
  目标应用、语音程序、语音按键和测试。原权限与诊断结果按职责并入对应业务行。
- 设备页“启动”不再保存其它未应用修改。按键恢复默认继续二次确认；语音程序新增
  Windows 语音输入（`Win+H`），并增加“试说一句”文字输入框。声音通道测试可在
  用户确认后临时正常退出正在运行的服务，测试结束自动恢复，恢复失败会明确提示。
- 删除旧连接、权限、诊断 QML 页面，新增三页共用的单行设置行。相关状态文案统一
  使用“遥控器服务”，错误指向设备页日志入口。
- 受影响范围共 418 项运行，416 项通过、2 项按平台条件跳过，无失败；真实 QML 在
  `720x464`、`640x440` 下无警告、横向溢出或区块重叠。本轮未构建或打包，状态为
  检查点待实测。

### 2026-08-27 冻结设置窗口启动修复

- 修复 PyInstaller 从构建环境 `PATH` 误收集其它工具链 `icuuc.dll` 的问题。冻结
  目录不再用错误 DLL 遮蔽 Windows 系统 ICU，`PySide6.QtCore` 可以正常加载。
- 新增冻结 `--qt-runtime-check`，真实导入设置窗口使用的五个 Qt 模块并确认
  `main.qml` 存在；本地构建脚本和 Windows CI 都把它列为必过门禁，避免原
  `--dry-run` 因 Qt 延迟导入而漏报。
- 旧 EXE SHA-256
  `2CE0AF478FCF1447312D65A4B00AEBC189743CD9DDB0DBC34F3F9D094BF1491F` 已确认
  无法启动设置窗口并作废。替代 EXE SHA-256 为
  `D6B037743321409AD878B3693EC76AD9347CEF70EE601CDC172EFC29363644C1`；完整构建
  1357 项测试中 1350 项通过、7 项跳过，冻结 Qt 检查和实际窗口启动均通过。

### 2026-08-27 设置界面交互与显示修复

- 修正左侧选中背景偏斜和点击切页迟滞：导航按钮不再维护第二套选中状态，按下时
  立即切页，背景边界与按钮边界完全一致。
- Windows 优先使用系统中文 UI 字体，解决复杂页面和弹窗中偶发的中文字形错配；
  系统字体不可用时仍回退到项目内置 Noto Sans SC。图标继续使用独立图标字体渲染。
- 13 张按键卡片高度由 42 增至 55，边框减细，非标题文字增大并加深；删除按键页
  底部重复的服务提示，默认窗口调整为 `720x500`，最小窗口调整为 `640x480`。
- “实际说话”改为至少 150 高的多行输入区并取消挤压它的标准底栏；按键编辑、快捷
  键录入和实际说话三个弹窗共用同一个 Fluent 关闭按钮。
- Qt 设置界面模块 182 项全部通过；Windows Fluent 实际窗口截图覆盖 `720x500`、
  `640x480` 的三页、实际说话弹窗和按键编辑弹窗。本轮未构建或打包。

### 2026-08-26 设置界面与语音程序整合

- 连接页改为紧凑双栏结构，把“语音程序”和“语音按键”放回连接流程；默认
  “不管理”，选择具体程序后随桥接尝试启动。桥接已经运行时，点击“保存并启动
  桥接”会直接应用新选择，不要求用户先退出桥接；程序缺失、UAC 取消或启动异常
  都不会影响 Remote Mic 与现有桥接。
- 按键页改为实物图、十三张映射卡片和互相避让的贝塞尔连线。卡片按“备注优先”
  显示两行内容；编辑弹窗分别保存按键录入和可选备注，语音动作下双击、长按保留
  可见但不可用。备注独立写入 schema 5 的 `display_notes`，不参与动作解析和执行。
- 保存映射继续原位更新模型，卡片、热点、连线和选中状态不会因保存重排。桥接启动
  进入等待设备阶段后，界面持续显示阶段、旋转状态和已等待时间；下拉框当前项使用
  浅色背景与粗体文字。
- 本次触达页面统一复用紧凑按钮、输入框、下拉箭头、文字层级和 Noto Sans SC 字体；
  `FluentWinUI3` 在 `720x464`、`640x440` 下完成四页和编辑弹窗截图复核，无默认
  滚动、裁切或错连。配置、保存辅助层、语音程序和 Qt 设置共 275 项通过，
  `compileall`、公开边界扫描 273 个文件与 `git diff --check` 通过。
- 本轮没有构建或打包，状态为检查点待实测。真实 RC003、输入法、UU 远程、音频
  端点和识别文字仍按 `TESTING.md` 验收。本节取代下方 2026-08-25 条目中的入口
  位置和手动启动交互；旧条目只保留历史。

### 2026-08-26 VB-CABLE 本地通道测试与音频时序

- “检查与修复”页新增用户主动触发的“VB-CABLE 通道”测试：只使用当前已保存的
  `CABLE Input` 和同一 host API 下唯一的 `CABLE Output`，发送短合成扫频并用
  归一化相关性确认信号实际穿过虚拟音频通道；不加入自动刷新，不保存 PCM，也不
  修改 Windows 默认设备。
- 测试与自动检查、设置保存和桥接启动互斥；桥接已经运行或状态未知时拒绝启动。
  测试全过程持有与桥接相同的 Windows 互斥，端点格式会逐项尝试真实双工打开；
  端点枚举、PortAudio 流打开/运行/关闭和结果分析全部放入一次性子进程。超时或
  设置窗口退出时会终止并确认子进程结束，避免原生音频调用卡住主程序退出。
- 正式播放日志新增输出流打开耗时、最近/最大阻塞写入耗时和欠载次数，便于区分
  “遥控器有 PCM”与“Windows 输出端写入发生阻塞”。受影响范围共 445 项，其中
  442 项通过、3 项按平台条件跳过；完整 unittest 共 1304 项，其中 1297 项通过、
  7 项跳过。
  真实 VB-CABLE 阈值和端到端文字上屏仍按 `TEST-VOICE-008` 实测，本轮未构建或打包。

### 2026-08-25 语音程序管理

- “按键”页把“按住说话快捷键”缩短为“语音快捷键”，并增加“语音程序”入口。
- 新增默认关闭的可选管理层：自动发现搜狗语音组件，也支持用户选择其它
  `.exe`/`.lnk`；可以手动启动或随桥接启动，失败不会阻止桥接。
- 管理员启动仅在用户明确勾选后请求 Windows UAC；Remote Mic 自身不提权，也不
  复制、安装或打包第三方输入法。搜狗、其它输入法和真实远程环境仍待人工实测。

### 2026-08-25 设置窗口单实例

- 设置窗口现在独立于桥接做单实例保护。重复点击程序、任务栏或通知区域入口时，
  新进程会退出并恢复、置前已有设置窗口，不再叠加打开多个窗口；关闭设置窗口仍
  不会停止后台桥接。

### 2026-08-24 自检补充

- **隔离迟到 legacy F5**：direct HID 已证明话筒键松开后若旧 F5 仍迟到，该来源
  会隔离到成功重连，不再把迟到 down/up 当成下一轮语音。F5 变换、设置应用和
  `AudioStopped` 收尾现由同一状态锁串行化；物理 HID 与“已决定但未 emit”的
  F5 也会原子选择唯一发送者，避免两个 down、一个 up。完整 unittest 1205 项
  通过、7 项跳过，应用接线 113 项通过、1 项跳过且无资源泄漏记录。
- **Codex 不上屏原因已确认**：当前 Codex/ChatGPT 以管理员级运行，搜狗语音为
  普通权限且 `UIAccess=0`；搜狗检测到前台窗口权限更高后拒绝文字输入。新建文档
  同为普通权限，所以同一音频链可用。本项只更新记录，未修改 Remote Mic、搜狗、
  Codex 或用户配置；普通权限 Codex 仍待人工复测。

### 构建

- 当前 `selfcheck1` 从 Git HEAD `43612e3b` 加本任务工作树构建；脚本内公开边界
  扫描 302 个文件、完整 unittest 1205 项通过和 7 项跳过，PyInstaller 与冻结
  `--dry-run` 通过。另行复核 `--help=0`，11 个源码/冻结 QML 零差异。
- 本机目录为
  `RemoteMicRC003-0.1.0-candidate-20260824-selfcheck1`，2072 个文件、
  320,960,337 字节；其中 2067 个运行文件与 `dist\RemoteMicRC003` 逐文件一致，
  另有 5 个便携版许可与说明文件。EXE 大小 5,053,936 字节，SHA-256 为
  `5AA9894E20FA05183BC2E5CEDDD342B148F36867262D899A9DD90CEEBCD174BF`。
- 便携 ZIP 为
  `RemoteMicRC003-0.1.0-candidate-20260824-selfcheck1-portable.zip`，
  大小 127,308,081 字节，SHA-256 为
  `B110C4D44AAD62060B597AF02D875D5D06576EA039F4187895AF4B2BC7AE94C4`。完整解压
  后只有一个版本顶层目录，2072 个文件与本机目录逐文件 SHA-256 一致。
- 哈希清单为
  `RemoteMicRC003-0.1.0-candidate-20260824-selfcheck1-SHA256SUMS.txt`。
  本包的真实 RC003、UU、搜狗文字上屏和普通权限 Codex 仍是检查点待实测。
- 从修复提交 `0b6d5aaa4d85fbab0add1252034cf0f3fd2967f0` 的干净源码运行正式
  构建门禁，公开边界扫描 278 个文件、完整 unittest 1165 项通过和 7 项跳过，
  PyInstaller 与冻结 `--dry-run` 通过。
- 当前本机候选目录为
  `RemoteMicRC003-0.1.0-candidate-20260823-hold4`，共 2062 个文件、
  320,902,537 字节；与 `dist\RemoteMicRC003` 零缺失、零新增、零内容差异。
  EXE SHA-256 为
  `64A329A51BE6C181FF4928463CD9DC6078A8B6C8B6A853EFD0379E75226CFCC4`。
- `--help=0`、`--dry-run=0`、无效 HID 注入 PID `=4`；6 个产品 QML 与源码一致。
  隔离配置目录中的冻结设置窗口离屏存活 5 秒，确认冻结 QML 可实际加载。
  Frida Gadget 和 VB-CABLE 固定资源哈希分别为
  `B566D70189B6D551AD8F4E0BEA24DE08A3D4C0F559BB35B2BDB67D45182240C2`、
  `B950E39F01AF1D04EA623C8F6D8EB9B6EA5C477C637295FABF20631C85116BFB`。
- `hold4` 未制作外发 ZIP；当前只完成自动验证和本机构建，不代表真实 RC003 已
  通过首次按键、桥接退出警告或按住说话验收。
- 此前 `hold3` 从修复提交 `83adfba` 的干净源码运行正式构建门禁，公开边界扫描
  290 个文件、完整 unittest 1157 项通过和 7 项跳过，PyInstaller 与冻结自检
  通过。
- 此前本机候选目录为
  `RemoteMicRC003-0.1.0-candidate-20260823-hold3`，共 2062 个文件、
  320,901,336 字节；与 `dist\RemoteMicRC003` 逐文件 SHA-256 一致。EXE SHA-256
  为 `8FF6A4840653035AA001746E0957159C33297BBBEA233AD9F6ED043112B38B0E`。
- `--help=0`、`--dry-run=0`、无效 HID 注入 PID `=4`；6 个 QML 与源码一致，
  新桥接警告已进入冻结资源，旧松手补发文字和 On-request 专项文件均为零。
- 便携 ZIP 为
  `RemoteMicRC003-0.1.0-candidate-20260823-hold3-portable.zip`，大小
  127,256,558 字节，SHA-256 为
  `33C53BB6AC59AFE8EC661D051AE60EA16290F882971E49F3D46BE6BAB697B1B2`。完整解压
  后只有一个 `hold3` 顶层目录，2062 个文件与本机候选逐文件一致。
- `hold3` 的上述记录只代表当时自动验证和构建，不代表已通过真机验收。

### 界面

- **撤回未经真机成立的三键默认**：`Alt + Space` 经 UU 会打开窗口系统菜单，
  但进一步实测也证明 `Ctrl + Alt + F8` 的按住边沿经 UU 后极不稳定，因此没有
  把它作为新配置默认发包。新安装、恢复默认和界面提示继续使用右侧 Alt；旧用户
  已保存的自定义组合键不会被覆盖。搜狗始终使用按住型，历史 `triggerType:free`
  来自另一条开关入口，不能据此声称用户切换过模式。TOGGLE 仅登记为待确认兼容
  提议，本次不恢复产品入口。
- **连接页下拉选项更易辨认**：“设备”和“语音输出”共用同一个单选下拉控件，
  展开后当前选中项加粗；唯一可确定的 CABLE Input WASAPI（无 WASAPI 时为
  DirectSound）在界面上标为“（推荐）”。标记不写入端点名、不自动选择，也不改变保存、
  桥接启动或音频路由。
- **编辑动作立即显示**：单击、双击和长按三个可编辑下拉框在选中预置动作后，
  立即同步输入框、弹窗状态和映射模型，不再等点击“完成”后才刷新。
- **统一未保存提示**：映射、按住说话快捷键、输出设备、设备选择和
  恢复默认发生实际变化后，底部显示红色“设置已修改，尚未保存。”；保存失败
  继续保留，保存成功才清除，具体错误始终优先显示。
- **桥接未运行警告**：设置窗口打开时核实后台桥接是否运行；连接页和按键页在
  未运行或无法确认时显示红色警告，明确话筒键此时不会由本程序触发语音。
  启动成功后提示首次连接和补充按键通道就绪可能需要约一分钟。
- **桥接退出后警告自动恢复**：主窗口现在每两秒核实一次后台桥接存活状态，并在
  窗口重新激活时立即检查。从通知区域退出桥接后，设置窗口无需重开或点击检测，
  两页红色警告会自动恢复；最近一次启动失败原因和 PID 结果不会被轮询覆盖。
- **补充通道第一次按键即可确认并捕获**：检测提示不再要求先等“已就绪”再按键。
  通道显示“已连接”后直接按一次即可；本地检测同样在 60 秒后超时释放。仅补充
  通道可用时改用完整已知按键表，不再只接受返回和音量键。

- **按键矩阵表头与数据列对齐**：表头和数据行原先使用两套独立
  `GridLayout`，数据行首列又会被内部布局撑宽，导致“单击 / 双击 / 长按”
  标题与实际内容明显错位。现在按键列和编辑列固定，中间的单击、双击、长按
  三列严格均分剩余空间；表头与数据行继续共用相同规则。两种主题和
  `1024x720`、`683x480`、`640x480` 三档窗口都通过真实 QML 坐标、等宽、
  非重叠及滚动条余量回归。
- **按键映射改为整宽矩阵**：移除旧的遥控器照片与双列卡片视图，13 个实体键
  统一按“遥控器按键 / 单击 / 双击 / 长按”四列展示。列标题强化，行间使用
  不贯通分隔线；真实按键检测继续定位并高亮同一行。每行“编辑”弹窗复用原有
  映射模型、快捷键录入和保存入口，语音主映射仍暂停但保留双击/长按设置。
- **统一设置框架**：窗口改为固定高度的顶部产品/页面导航、独立滚动内容区和
  唯一的底部反馈条。保存、打开系统设置等普通结果使用中性反馈，真实错误优先
  显示；按键页不再重复渲染同一组状态文字。关闭设置窗口仍不结束后台桥接。
- **连接页按实际工作流重排**：顺序统一为设备、RC003 语音输出、后台桥接
  启动结果与保存/启动命令；“仅保存设置”和“保存并启动桥接”继续调用原有两个
  独立入口，不合并语义。启动结果明确不是 RC003 实时连接状态，恢复入口改为
  准确的“恢复按键与语音默认”。
- **权限页按责任边界重排**：分为运行必需、可选增强、手动操作和问题排查。
  RC003 才显示蓝牙、HID tap、VB-CABLE 与宿主语音快捷键说明；DJI Mic 2
  显示自己的 Windows 录音输入说明。页面不伪造“已授权”状态，不复制驱动安装
  流程，也不把管理员权限说成整个程序的必需条件。
- **响应式与键盘操作**：页面内容限制最大宽度并禁止横向滚动；窄窗口中使用
  Tab 移动到视口外控件时会自动滚动，末项仍可回到顶部导航，不形成页内焦点
  陷阱。现有按键矩阵、BLE 和音频链路不在这轮界面重排范围。

### 修复

- **默认值不能遮住旧嵌套快捷键**：配置加载现在先归一化磁盘中真实存在的语音
  字段，再合并默认，避免只有 `voice_hotkeys.hold`、没有顶层 `voice_hotkey` 的
  老文件被误改。旧开关型、Ctrl+Win 和错误 `lalt` 的历史迁移目标继续是右侧 Alt；
  通用三键的发送顺序、反向释放和失败补释放测试仍保留，但它不再是默认值。
  UU 按住边沿不稳定的问题见 `TEST-VOICE-007`，当前没有宣称已解决。
- **旧语音配置让话筒键只输入日期**：核实被测机仍保存 schema 1 开关型配置，
  旧话筒动作没有成功重选保存，后台桥接也未启动；因此原始话筒键进入前台，
  这段证据不是新版按住说话链路启动后失效。后续 2026-08-23 15:26 日志证明
  新版桥接后来已收到有效语音数据并重新加载设置。程序继续拒绝自动转换旧
  开关型配置，错误改为显示“话筒键”，要求用户明确选择并保存启动。
- **无收益的松手补发造成产品歧义**：后续有效日志多次证明补发快捷键已经排队
  并发送，但没有带来可靠宿主效果。现已删除界面开关、配置字段、计时器和延迟
  点按；旧字段为 `true` 也不会触发额外动作。
- **真实按键检测临时少键时提示不清**：补充按键连接关闭时，界面明确说明仍可
  检测 Windows 直接识别的按键，但返回键、音量键等可能暂时测不到；补充通道
  恢复为“已连接”后直接按一次特殊键，不再循环等待“已就绪”。检测状态不直接
  显示内部通道代号。
- **冷启动时首次按键无响应或第二次才成功**：日志确认 17:58 桥接未运行；
  18:00:37 启动后，补充按键通道到 18:01:23 才首次就绪。检测界面现在区分
  Windows 通道已启动、补充通道连接中和两条通道均就绪，后台一次性检测等待由
  15 秒延长为 60 秒。18:01 后的真机日志已确认按键快速捕获且两轮语音为有效
  信号；新界面提示仍待 `hold3` 真机复测。

- **长按遥控器方向键时原始方向键漏到前台**：旧拦截器只吞掉实体按下和抬起
  各一条记录，Windows 在长按期间每约 60 至 80 毫秒生成的方向键重复会绕过
  映射，造成光标先移动、再输入配置的组合键。现在按设备侧报告保存按住状态，
  按住期间吞掉同一实体键的全部重复；设备报告松手后继续清掉已经排队的重复，
  收到 Windows 对应抬起后立即解除。软件注入的映射键、其他键和普通键盘输入
  不进入这条状态。
- **专项检测连接卡死且无处退出**：旧候选的八秒截止生效后，正式蓝牙连接仍
  可能无限等待，持续占用后台位置和按键拦截，使主程序没有通知区域图标、真实
  按键检测也无法启动。现在连接准备阶段最长 32 秒，进入按键观察后另有 25 秒
  收尾截止；超时会自动停止拦截、释放后台、写入明确结果并结束程序。
- **短语音停止越过已收到音频**：CONTROL/AUDIO 分队列原本让
  `AUDIO_STOP` 优先关闭 decoder，排队中的短流尾包随后全部被判为晚到数据。
  现在两个队列共享通知序号，序号分配与入队由 producer lock 原子保护；STOP
  只排空同代且更早的音频，不吞下一会话首帧。确定性、跨会话和并发 producer
  回归通过；真实 RC003 短流仍待候选复测。

- **语音触发但无声**：过滤当前 PortAudio 阻塞播放不支持的
  `Windows WDM-KS` 端点；同名视图优先 `Windows WASAPI`，缺失时使用
  `Windows DirectSound`；设置保存和诊断页自动选择都会先真实打开、启动
  并关闭输出流，不能再把只能枚举、无法播放的端点写入配置。已有 WDM-KS
  配置且检测到标准 CABLE Input 时，设置页预选其 WASAPI 视图，等待用户
  保存后再持久化。
- **部分按键在设置页无法识别**：设置页的真实按键检测同时接收 Raw Input
  与 Frida HID tap，tap 只补齐 Windows 普通键盘链路丢失的返回和音量
  usages，任一来源捕获到首个按下事件后统一停止且不执行映射动作。
- **HID tap 假就绪与日志缺失**：注入改为隐藏的独立子进程，保留 PID、
  WUDFHost 和 Gadget 哈希校验；新增等待宿主、注入、等待连接、等待 IO、
  ready、不健康和失败状态。后台线程创建只记录 starting，收到有效 HID IO
  后才记录 ready，状态进入 `app.log` 而不是窗口 EXE 不可见的 stdout。
- **重复启动提示与状态矛盾**：设置页启动桥接时附加隐藏来源标记；已有
  桥接实例时，子进程不再弹出阻塞轮询的系统模态框，而是立即返回既有的
  确定退出码，设置页可准确显示“已经在运行”。手工 `--bridge` 启动仍保留
  可见的单实例保护提示。
- **后台桥接缺少退出入口**：桥接进程新增 Windows 通知区域图标；双击可
  打开设置，右键可选择“打开设置”或“退出桥接”。托盘退出会投递到 asyncio
  主线程并执行既有 BLE、HID、语音热键、音频与互斥锁清理，不依赖任务
  管理器强杀。关闭设置窗口仍只关闭设置，不会意外中断桥接。
- **切换模式短按后没有持续语音**：切换状态现在只由两次独立按下改变；
  第一次松开产生的 `AudioStopped` 不再关闭宿主，而会续发 `MIC_OPEN`；
  第二次按下发送宿主关闭快捷键和线程安全 `MIC_CLOSE`，竞态音频事件不会
  重新开麦。按住说话模式保持原行为。
  后续产品复核撤回“持续语音已修复”的判断：上述只是 Windows 状态机行为，
  RC003 主动续开会话仍可能零 PCM；TOGGLE 现为 `BUG-017` 的实验路径。
- **从托盘打开设置后真实按键检测失效**：设置页检测到后台桥接已持有 HID
  资源时，通过一次性本地文件 IPC 请求后台捕获下一键；后台回传逻辑按键并
  吞掉该次 down/up，不执行映射。后台未运行时仍使用本地 Raw Input 与
  HID tap。
- **配对列表一个设备但桥接报告两个候选**：先用隐私安全日志确认 Windows
  配对数据库保留了两个不同 RC003 记录；精确删除隐藏 PnP 节点后系统扫描
  仍会重建。桥接现在只在多候选时逐个执行 UNCACHED ATVV 语音服务探测，
  恰好一个可用才连接；零个或多个可用仍失败关闭，不按名称或顺序猜测。
- **旧蓝牙候选让专项检测长期占用后台**：Windows 蓝牙调用面对旧配对记录时
  可能不响应取消，使原来的八秒超时失效。真实候选检查现改在独立守护线程中
  运行，主循环按真实八秒截止并继续检查其他候选；同一失效候选不会反复创建
  线程。占用提示也会区分后台桥接和上一次专项检测。
- **切换模式仍像按住说话**：同一次实体麦克风按下会分别产生
  `AudioStarted`、legacy F5、HID 和 ATVV mic 事件，旧局部门闩会把后到
  来源当成第二次点击并立即 `MIC_CLOSE`。现在首个来源认领整轮手势，其他
  来源只合并不再切换；物理/音频释放后下一次按下才执行关闭。HOLD 模式的
  一次 key-down/key-up 行为保持不变。
- **切换模式时灵时不灵且偶发输入日期时间**：`AudioStopped` 会早于排队中
  的 F5/HID 物理释放，门闩不能只靠音频停止释放；低层 F5 钩子也不能同步
  等待 PortAudio 初始化。现在手势需同时越过音频停止和全部物理 up，F5
  应用处理投递到事件循环，重复音频 start/stop 只处理一次；关闭切换时不再
  重开播放端点。
- **设置语音模式后运行中行为没有变化**：设置窗口会保存
  `voice_trigger_mode` 与 `voice_hotkey`，但桥接只热加载按键映射，导致界面
  选择 HOLD 后日志仍执行 TOGGLE。桥接现在同时监控两个设置文件；空闲时在
  下一次输入事件前立即应用语音模式和快捷键，活跃会话则在完整收尾后切换。
  启动和热应用日志会写明实际生效模式与快捷键。
- **检测麦克风键后后台持续空开麦**：`AudioStarted` 或 ATVV mic 事件可能
  比 F5/HID 的检测回调先到，旧逻辑会先改变语音状态，再把后到物理边沿作为
  检测结果吞掉。现在一次检测手势由首个来源统一认领，音频、ATVV、F5 和
  HID 后续事件全部只用于收尾，不发送宿主快捷键、播放端点或设备开关麦命令；
  释放宽限和硬超时确保乱序事件被吸收且下一次正常按键仍可用。
- **切换模式第一次短按后无声、必须先长按**：真机日志确认
  `AudioStopped` 到达时实体 F5 仍处于 down；旧逻辑立即续发 `MIC_OPEN`，
  设备虽回复 `AudioStarted` 却不再发送 PCM。现在续开先等待全部实体来源
  up；BLE-only 路径使用 200 毫秒有界宽限吸收迟到边沿。第二次点击、清理和
  重连都会取消待续开，旧回调不能串入新连接。
- **真实按键检测偶发没有结果**：多个 HID/F5/音频来源会同时读取一次性请求
  后再竞争认领锁；Windows 文件共享时序可能让锁胜者移动失败、败者也返回
  失败。现在先独占 claim lock 再读取和移动请求，结果写入失败会尽量恢复
  请求供后续来源重试。
- **切换续开后约 60 毫秒又被关闭**：`fix13` 真机日志确认程序在实体松键后
  续发 `MIC_OPEN`，紧接着出现一组新的 legacy F5 down/up，被误判成第二次
  点击并产生空会话。现在续开后设置 250 毫秒有界回声保护，同源 down/up
  成对吞掉；保护期后的真实第二次点击仍正常关闭。
- **PortAudio 清理失败时丢失重试句柄**：播放流只有在 `close()` 确认成功后
  才清除内部引用；关闭失败会保留句柄供后续清理重试。
- **遥控器松手晚于键盘约两秒才结束宿主快捷键**：真机日志显示直接 HID
  话筒 usage 已在 `20:51:30.109` 抬起，但 legacy F5 仍持续自动重复，直到
  `20:51:31.959` 才出现 up。第一轮 HOLD 修复仍等待全部重复来源，因此没有
  真正按物理松手释放。现在直接 HID up 被视为权威松手，立即发送 KEY_UP；
  晚到的 F5 up 和 `AudioStopped` 只收尾，不重复释放。
- **独立语音能力探针向记事本写入日期**：旧探针退出桥接后没有启动正式桥接
  的 F5 guard，RC003 话筒键会作为原生 F5 到达前台。探针现仅在自身生命周期
  内临时吞掉 F5，不替换成宿主快捷键；guard 无法启动时停止诊断，不再用受
  污染的操作下能力结论。
- **隐私字段大小写绕过**：禁止持久化的设备标识字段改为大小写不敏感检查，
  不能通过改变 JSON 键名大小写绕过。
- **开始菜单环境缺失时误扫工作目录**：`APPDATA` 或 `PROGRAMDATA` 不存在时
  不再把相对路径解析为当前工作目录，避免意外启动同名快捷方式。

### 变更

- RC003 正式语音能力收敛为实体话筒键 HOLD。设置界面删除开关型快捷键和动作，
  其他按钮不再提供遥控器语音动作；话筒键仍可改成普通动作。
- 配置升级到 schema 3。旧开关型、非话筒键语音和次级语音配置整键停用，要求
  用户明确重新选择；程序不会把旧开关型自动解释成按住说话。schema-2 的松手
  补发字段在加载和保存时移除。
- 语音结束路径只保留标准快捷键抬起。实体松手和音频停止仍互为结束兜底，后到
  的事件不会重复抬键，也不会再安排第二次快捷键点按。
- 长按重复改为动作白名单：方向、退格和音量加减保留连续操作，自定义组合键
  每次实体按下只执行一次完整点按。
- 删除已经完成使命的 `--on-request-probe` 生产入口、实现模块和便携启动脚本；
  协议常量与 `BUG-017` 保留为历史证据。
- `fix13` 和 `fix15` 曾实现双快捷键、任意实体键语音等实验界面；这些阶段记录
  仅用于追溯，已被本批 HOLD-only 产品边界取代。
- 普通麦克风映射与非麦克风语音映射都按事件来源合并重叠和迟到边沿，避免
  HID tap、Raw Input 与 legacy F5 把一次实体操作执行两次；热加载到损坏配置
  时会立即关闭旧 F5 语音转换快照，修复配置前保持失败关闭。
- 设置页保存提示区分热应用与重启边界：按键映射、显式语音动作和两套快捷键在
  下一次输入事件时应用；连接设备和音频输出端点仍需重启桥接。

自动验证与候选：

- 本批 Qt 设置页 109 项通过；配置、保存模型、运行时和设置页四组定向测试
  303 项通过、1 项跳过；完整 unittest 1152 项通过、7 项按平台或安全条件
  跳过，退出码 0。
- `compileall`、`pip check`、PowerShell parser、`git diff --check` 通过；公开
  边界扫描 289 个文件通过。
- 从源码提交 `90b552e` 构建本机候选
  `RemoteMicRC003-0.1.0-candidate-20260823-hold2`。目录含 2062 个
  文件、320,898,562 字节，与构建输出逐文件 SHA-256 一致；`--help=0`、
  `--dry-run=0`，六个产品 QML 与源码一致且不含已撤下控件。EXE SHA-256 为
  `AE072B578FFA112F301201D8452C8A8959C656D75977BF00620A5E066819C5FF`。
- 用户随后要求制作便携 ZIP：
  `RemoteMicRC003-0.1.0-candidate-20260823-hold2-portable.zip`，大小
  127,255,338 字节，SHA-256 为
  `3D9E900195FE99D193D4C324D09D993B1F94146C4821456B62F1EF05527D44FB`。
  完整解压后只有一个 `hold2` 版本顶层目录，2062 个文件与本机候选逐文件
  SHA-256 一致；制作 ZIP 不等同于真机功能已经通过。
- 当前 HOLD-only 收口核心定向测试 481 项通过、1 项按平台条件跳过；完整
  unittest 1151 项通过、7 项按平台或安全条件跳过，退出码 0。
- `compileall`、`pip check`、PowerShell parser、`git diff --check` 通过；公开
  边界扫描 278 个文件通过。QML 离屏加载和矩阵布局检查包含在完整测试中。
- 从源码提交 `b74288c` 构建本机候选
  `RemoteMicRC003-0.1.0-candidate-20260822-hold1`。目录含 2062 个
  文件，复制后逐文件 SHA-256 一致；EXE SHA-256 为
  `752AB73FD3A6BF841682C2D41ADDACDE9520F22CBAC438AB19A8EFA8612970C8`。
  冻结入口 `--help=0`、`--dry-run=0`，六个 QML 文件与源码一致，Frida Gadget
  和 VB-CABLE 固定资源校验通过，旧 On-request 专项文件为零。本批不制作外发
  ZIP，等待用户先验收本机程序。
- 用户确认本机程序后，于 2026-08-23 生成便携 ZIP
  `RemoteMicRC003-0.1.0-candidate-20260822-hold1-portable.zip`，大小
  127,256,811 字节，SHA-256 为
  `9A3A9B09D60B360C3C55F14BBB800AFEB0FE3E5B4C446A985CAD812E024D861D`。
  完整解压后只有一个版本顶层目录，2062 个文件与候选目录逐文件 SHA-256 一致。
- 完整测试：1006 项通过，7 项安全或平台相关跳过，退出码 0。
- 公开边界扫描：227 个文件通过；`compileall`、`pip check` 与
  `git diff --check` 通过。
- PyInstaller 构建和冻结入口检查通过：`--dry-run=0`、`--help=0`、无效
  HID 注入 PID 入口 `=4`。
- `fix9` 候选目录：源码仓库同级的
  `RemoteMicRC003-0.1.0-candidate-20260820-fix9/`。
- `fix9` 候选 EXE SHA-256：
  `DB3F49E23F2B6EEB6C51B9B0B45C5A2E1B29EEEA2E8FA97FFB999A49A3E91C28`。
- `fix9` 便携 ZIP SHA-256：
  `F24AFA07020385276297537E6B3F3F49880A305BC019460D3849778835CC778C`；
  ZIP 内 EXE 已重新计算并与候选目录一致。
- `fix10` 代码修复检查点：`eafd203`；构建时 Git HEAD：`041c125`。
- `fix10` 完整测试：1090 项通过，7 项安全或平台相关跳过；公开边界扫描
  228 个文件通过；PyInstaller 构建通过。
- `fix10` 冻结入口：`--dry-run=0`、`--help=0`、无效 HID 注入 PID `=4`。
- `fix10` 候选目录：源码仓库同级的
  `RemoteMicRC003-0.1.0-candidate-20260821-fix10/`。
- `fix10` 候选 EXE SHA-256：
  `B499B7C665115B128FFAF49F80A9A425B82969157A4F0C6EA2ABCFA784BA15B2`。
- `fix10` 便携 ZIP SHA-256：
  `69B3F76BB2590C736CFEE6359AADE5EB8848DB2937D7FDC8CDAF404F70708799`；
  ZIP 内 EXE 已重新计算并与候选目录一致。
- `fix11` 麦克风检测隔离代码提交：`4d6a0fe`。
- `fix11` 构建前完整测试：1093 项通过，7 项安全或平台相关跳过；公开边界
  扫描 229 个文件通过；`compileall`、`pip check` 和 `git diff --check` 通过。
- `fix11` 构建时 Git HEAD：`c135991`；PyInstaller 构建通过，冻结入口
  `--dry-run=0`、`--help=0`、无效 HID 注入 PID `=4`。
- `fix11` 候选目录：源码仓库同级的
  `RemoteMicRC003-0.1.0-candidate-20260821-fix11/`。
- `fix11` 候选 EXE SHA-256：
  `4639AC9942C3D66525ED8EC7457F1EA7562E1B9DBD14ED5E5FD05BB2CB44ED7D`。
- `fix11` 便携 ZIP SHA-256：
  `DF9F50C4B9F11D703FA65BFE200A52FBD953B7591670FA3AEF89B6C96985127B`；
  ZIP 内 EXE 已重新计算并与候选目录一致。
- `fix12` 实体释放后续开代码提交：`f6ae3c3`；并发检测认领提交：
  `cfbca35`。应用接线测试 83 项通过、1 项平台相关跳过；检测与应用扩展
  定向测试 90 项通过、1 项跳过；完整测试 1096 项通过、7 项跳过；公开边界
  扫描 231 个文件通过。
- `fix12` 构建时 Git HEAD：`62b1032`；PyInstaller 构建通过，冻结入口
  `--dry-run=0`、`--help=0`、无效 HID 注入 PID `=4`。
- `fix12` 候选目录：源码仓库同级的
  `RemoteMicRC003-0.1.0-candidate-20260821-fix12/`。
- `fix12` 候选 EXE SHA-256：
  `7B99267D5E1385408CDBAAE36BA422FAACC43B50F16E47BF3E5EEC8176BB0926`。
- `fix12` 便携 ZIP SHA-256：
  `D6B9168B728E12FD0D892BD472547007F0A8B5E835F34D35230677E76BF5CBAA`；
  ZIP 内 EXE 已重新计算并与候选目录一致。
- `fix13` 界面代码提交：`459a0bd`。配置、设置控制器与 QML 定向测试
  174 项通过；完整测试 1102 项通过、7 项安全或平台相关跳过；公开边界扫描
  231 个文件通过；`compileall`、`pip check` 和 `git diff --check` 通过。
- `fix13` 构建时 Git HEAD：`63b038d`；PyInstaller 构建通过，冻结入口
  `--dry-run=0`、`--help=0`、无效 HID 注入 PID `=4`。
- `fix13` 候选目录：源码仓库同级的
  `RemoteMicRC003-0.1.0-candidate-20260821-fix13/`。
- `fix13` 候选 EXE SHA-256：
  `DB7C357419DACDC04491C50CF3BD5AFF4353467B30BE3B5E73E5C3012EC34CA6`。
- `fix13` 便携 ZIP SHA-256：
  `B496F8DEED84DC7D8A2B28E711931EDEC11BC169A93771F56732A6339849D4FE`；
  ZIP 内 EXE 已重新计算并与候选目录一致。人工界面与真机语音仍待复测。
- `fix14` F5 续开回声代码提交：`bfd514d`。应用接线测试 84 项通过、1 项
  平台相关跳过；完整测试 1103 项通过、7 项跳过；公开边界扫描 231 个文件
  通过；`compileall`、`pip check` 和 `git diff --check` 通过。
- `fix14` 构建时 Git HEAD：`f5c86b9`；构建门禁公开边界扫描 233 个文件，
  PyInstaller 和冻结入口检查通过。
- `fix14` 候选目录：源码仓库同级的
  `RemoteMicRC003-0.1.0-candidate-20260821-fix14/`；EXE SHA-256 为
  `E0E3A7A36D6F07A1E1893E6B2A2750240EE0C78056EE5DC6DCE331227C98D24D`。
- `fix14` 便携 ZIP SHA-256：
  `59DA357C40BDA873AF7FF06F4CFFFC68D1E5EB56FEC4BE37B3CE3BAA453FCDB5`；
  ZIP 内 EXE 已重新计算并与候选目录一致。真机复测待完成。
- `fix15` 检查点提交 `979ed49`，最终代码提交 `eaabde8`。应用接线测试 108 项
  通过、1 项平台相关跳过；完整测试 1143 项通过、7 项跳过；1024x720 离屏
  QML 加载 0 warning；公开边界扫描 266 个文件通过；`compileall`、`pip check`
  和 `git diff --check` 通过。
- `fix15` 构建时 Git HEAD：`8624ceb`；PyInstaller 与冻结入口检查通过，
  `--dry-run=0`、`--help=0`、无效 HID 注入 PID `=4`。
- `fix15` 候选目录：源码仓库同级
  `RemoteMicRC003-0.1.0-candidate-20260821-fix15/`；EXE SHA-256 为
  `FE9B15C484DCF5E7DCBC2472DA3181587344AB4ABA3F942FC2C972B20475D61D`。
- `fix15` 便携 ZIP SHA-256：
  `6A24FA1023F5E4C5AC4FFCE68C43B2334286851A69CB7C87E00F1C1E60EF7DF`；
  ZIP 内 EXE 已重新计算并与候选目录一致。统一映射与非麦克风键语音仍待
  真机复测。
- `ui1` 全局框架、连接页和权限页代码提交：`addc2b0`。Qt 设置定向测试
  101 项通过；从该提交的干净 detached worktree 执行完整测试 1151 项通过、
  7 项安全或平台相关跳过；公开边界扫描 137 个文件通过；`compileall`、
  `pip check` 和 `git diff --check` 通过。共享工作区开发期的 1154/7 和
  267 文件结果包含同期 BLE 测试，不作为 UI 独立候选的权威计数。
- `ui1` 候选目录为源码仓库同级
  `RemoteMicRC003-0.1.0-candidate-20260821-ui1/`；EXE SHA-256 为
  `8575F8B3FD273F5CC6915D2F8A9BEA037D0E66E27EF6929AAE15887E6ACB0988`。
  便携 ZIP SHA-256 为
  `9E3D8ECBFD95335720E80DE853E7BCBD97D4AC3D02FCC314FF456580E37EC718`；
  ZIP 内 EXE 与目录 EXE 一致，六个冻结 QML 与 `addc2b0` 源码逐文件一致。
  冻结入口 `--dry-run=0`、`--help=0`、无效 HID 注入 PID `=4`。该 UI-only
  候选不包含随后提交 `a189a1c` 的 BLE/音频停止排序修复；后续逐文件复核还发现
  它未包含可选 Frida Gadget，因此 `ui1` 只保留作历史 UI 证据，不再继续交付。
- 修正合并候选 `ui2` 从 `338d2cb` 构建，包含 `addc2b0` 界面与 `a189a1c`
  音频停止排序修复。构建门禁 1154 项通过、7 项跳过，公开边界扫描 270 个文件
  通过；PyInstaller 和冻结入口验证通过。
- `ui2` 候选目录为源码仓库同级
  `RemoteMicRC003-0.1.0-candidate-20260821-ui2/`；EXE SHA-256 为
  `03E21A6E28A3205E35A3C039A4BD681658BD0AB261DFD6C8DD3D944173561902`，
  便携 ZIP SHA-256 为
  `5330E2E668471E60F3AE5CEC1FBCBB172D844DFE1E49C383F8A1F51FC9383B81`。
  Frida Gadget 与 VB-CABLE 均存在且固定哈希通过；ZIP 和目录 2067 个文件逐项
  一致，冻结 QML 与许可/说明文件也与源码一致。人工界面与真机语音复核待完成。
- On-request 专项探针定向测试 244 项通过；完整测试 1171 项通过、7 项安全或
  平台条件跳过，公开边界扫描 276 个文件通过；`compileall`、`pip check` 和
  `git diff --check` 通过。真实 RC003 结论仍待便携诊断包运行结果。
- 探针源码检查点为 `dc82e1a`。便携目录
  `RemoteMicRC003-0.1.0-candidate-20260822-onrequest-probe1/` 与同名 ZIP
  均含 2071 个文件且逐文件一致；EXE SHA-256 为
  `B5089C4DA3BFFB6823D680C62F428B95D49D4F14C0D6BF3ACC0001E4DDF1C5E1`，
  ZIP SHA-256 为
  `8CE01906978263AB93CFD16A128FE6ACE85DB0C1592885060759773E71A5B059`。
  冻结 `--dry-run=0`，真机探针仍待运行。
- 为异机配置与测试另行组装
  `RemoteMicRC003-0.1.0-candidate-20260822-testkit1/` 与同名 ZIP。程序本体
  未重新编译，EXE 与 `onrequest-probe1` 的 SHA-256 相同；包内新增
  `配置与测试说明.md` 并重新生成 `SHA256SUMS.txt`。
- `testkit1` 目录和 ZIP 均含 2072 个文件且逐文件一致；除新增说明和校验清单
  外，其余 2070 个文件与 `onrequest-probe1` 逐文件一致。ZIP SHA-256 为
  `BC7D9FCEDCF2BC438630B5382661111F6669810B691569D5BC4D6973334D9BB0`。
  冻结 `--dry-run=0`，目标电脑上的 RC003、语音链路和专项探针仍待人工测试。
- `testkit1` 保留作旧界面诊断历史，不再作为当前界面外发包。整宽矩阵源码
  提交为 `ae3529e`；Qt 设置测试 102 项通过，完整构建测试 1172 项通过、7 项
  跳过，公开边界扫描 276 个文件通过，PyInstaller 与冻结 `--dry-run=0`。
- 当前外发包为
  `RemoteMicRC003-0.1.0-candidate-20260822-matrix1-portable.zip`；目录和 ZIP
  均含 2072 个文件且逐文件 SHA-256 一致，只有一个同名版本顶层目录。EXE
  SHA-256 为
  `6252F78F5BD66DF514AB0028966640A3BCB00F83FE58C06D18D73C154338B6A3`，
  ZIP SHA-256 为
  `5232C3B6EC0885956FC15EB41833B947F56EA9512B30450262B63E7EBE74E11C`。
  本机人工确认矩阵界面显示正确；真实 RC003、语音和 On-request 结果仍待测试。

## [0.1.0-candidate] — 2026-07-31

标签：`v0.1.0-windows-rc003-candidate.1`（基于 `6c33fcc`）

首个 Windows RC003 候选发布。本版本已在真实 RC003 遥控器上完成逐键、
语音链路验收（详见 README“真机验收”部分）。CI 与自动构建仍然不能
替代真机验证。

### 新增

- **Frida HID tap 旁路**：对 Windows 普通输入链路拿不到的返回、音量+、
  音量- 缺失 usages（`0xF1`、`0x80`、`0x81`），复用上游
  `remote-bridge-hub` 的 Frida Gadget WUDFHost tap 读取；扩展为上报
  遥控器全部键盘 usage，作为所有普通按键的输入旁路。Gadget 是可选的
  第三方二进制，需显式获取（`build/fetch-frida-gadget.ps1`）且验证
  固定 SHA-256 后才会启用。
- **豆包语音触发（DoubaoPhysicalizer）**：注入的右 Alt 合成事件此前被
  豆包输入法 `ImeService` 的低层键盘钩子以 `LLKHF_INJECTED` 标志忽略；
  现在附加到 `ImeService.exe` 的低层回调，只对该标记事件清除 injected /
  lower-integrity 标志并清空 `dwExtraInfo`，使豆包看到的按键形状与
  实体右 Alt 一致。默认按住模式 `ralt`、切换模式 `ralt+space`。
- **设置页独立入口**：`RemoteMicRC003Settings.exe` 与桥接 EXE 分离
  （后合并为单个 EXE，见下）。
- **按键采集/回放工具**：`src/rc003_key_test.py`、`rc003_key_probe.py`
  等诊断工具，被动记录真实物理签名，不执行映射动作。

### 修复

- **普通按键双触发**：方向键、OK 等按键按下时一次动作被触发两次。
  根因是低层键盘钩子阻塞了 `WM_INPUT` 派发，导致“先 arm 后吞键”的
  等待式方案永远慢半拍。改为由 Frida GATT tap 的独立 socket 线程在
  `NtDeviceIoControlFile` 报告到达时 arm，低层钩子零等待匹配并吞掉
  原生按键，只注入一次映射动作。方向/OK/Home/Menu/TV/Power/返回/
  音量键全部实测通过。
- **F5 语音键重复替换刷屏**：按住麦克风键期间键盘 auto-repeat 会让
  “替换为右 Alt”逻辑反复触发；为 transform 增加已按下/已发送守卫，
  只在真实按下/释放边沿各发送一次。
- **BLE GATT 特征找不到**：修复后反复出现
  `ATVV characteristic not found`；改用 `BluetoothCacheMode.UNCACHED`
  读取服务与特征，避免 Windows 缓存旧枚举结果。
- **设置保存失败且映射不生效**：配置文件改为临时文件 + fsync +
  `os.replace` 原子写入；Qt 设置保存捕获一切持久化/回读异常并在界面
  显示错误；桥接进程在按键前按 mtime 热加载新的按键映射，磁盘数据
  损坏时保留最后一份有效映射。
- **启动闪黑色命令行窗口**：桥接启动子进程与打包运行时的控制台子进程
  均使用 `CREATE_NO_WINDOW` 隐藏。
- **语音识别无声/不稳定**：语音输出改为按端点能力输出立体声并复制
  声道；解码后增加 20 Hz 一阶高通 DC 阻挡；默认增益提高到 +10 dB；
  16 kHz → 48 kHz 改有状态连续插值（对齐上游）。实机验收：豆包输入法
  能识别遥控器语音。

### 变更

- **单 EXE 行为**：合并为同一个 `RemoteMicRC003.exe`。双击（无参数）或
  `--settings` 打开设置窗口；`--bridge` 显式启动桥接进程。安装器/便携版
  的启动快捷方式统一使用 `--bridge`。
- **设置保存原子化**：`save_config` / `save_key_bindings` 走原子写入，
  不暴露半写的 JSON。
- **返回键默认映射**：保持 `delete_backward`（退格）语义；新增可选的
  “浏览器后退”动作供用户在设置页手动绑定。
- 普通按键仍通过 `SendInput` 注入映射动作；语音快捷键通过物理化的
  右 Alt 事件；两者互不混用。

### 已知限制

- 未签名，首次运行会触发 SmartScreen 提示，属预期行为。
- Frida Gadget 与 VB-CABLE 均为可选第三方组件；未显式获取/安装时，
  缺失 usages 不会被猜测伪造，语音默认没有虚拟麦克风路由。
- 遥控器没有独立物理静音键；“系统静音”只是可选手动绑定。
- 安装器与便携版运行期配置都写入 `%LOCALAPPDATA%\RemoteMic\RC003`，
  卸载不会自动删除。
