# 原作者界面与功能引入推演

日期：2026-08-24

状态：方向已部分确认，第 1、2 项明确考虑，其余待决策，暂不实施

范围：Windows RC003 设置界面、按键动作、语音辅助功能与后续产品能力

## 1. 文档目的

本文记录既有 macOS 客户端中可供 Windows RC003 评估的界面源码、
现有功能差异，以及哪些能力适合引入 Windows、哪些需要分阶段验证、哪些暂时
不应进入当前产品范围。

本文不是需求承诺、实现设计或发布计划。文中所有候选能力都没有进入 Windows
配置格式、运行时、设置界面、测试基线或候选包；后续实施前仍需按影响级别和
交互变更规则单独确认。

2026-08-24 用户确认：

- 第 1 项“界面框架、连接就绪摘要和关于页”进入明确考虑范围；
- 第 2 项“向当前选定端点发送 1 秒测试音”进入明确考虑范围；
- 本文记录的其它功能统一保持“待决策”，当前既不排期实施，也不作最终否决。

## 2. 已核实的参考来源

2026-08-24 只读核实到：

- 原作者公开仓库：`HD838A/remote-mic-app`；远端 `main` 为
  `f3e0f26c5c6c406be7f0e989c816390ee7cc0b9e`；
- 本地 `remote-mic-app` 位于
  `4d526175817ad2c4c5fbe1650d528c946a3cdbf3`，落后远端 `main` 51 个提交，
  因此本地文件便于查看结构，但当前事实以远端 `main` 为准；
- 既有产品截图展示连接与语音、
  按键映射、统计、权限与隐私等页面；
- App 源码按 GPL-3.0-only 发布，但 Logo、App Icon、官网设计、文案和截图不因
  源码开源而自动获得复用授权。本项目只参考信息架构、交互方法和开源代码，
  不复制品牌资产或官网页面。

最直接的界面代码入口：

- `Sources/RemoteMic/SettingsView.swift`：侧边导航、连接、统计、权限、关于页，
  以及页面标题、状态标签和面板等公共视觉结构；
- `Sources/RemoteMic/RemoteMappingCanvas.swift`：遥控器居中、两侧按键卡片、
  连线、选中态和响应式布局；
- `Sources/RemoteMic/SettingsScreenshotRenderer.swift`：固定状态和窗口尺寸的
  设置页截图生成；
- `Sources/RemoteMic/TestTone.swift`：测试音生成与播放门禁；
- `Sources/RemoteMic/BridgeAppModel.swift`：统计、测试音、手机/网页接力、
  回眸和组合动作等功能的模型入口；
- `Sources/RemoteMic/TranscriptCaptureCoordinator.swift`、
  `TranscriptArchiveStore.swift`：回眸文字的捕获和本地保存；
- `Sources/RemoteMic/MacroFeatureIntegration.swift`：组合动作和映射方案的隔离
  接口；
- `Sources/RemoteMic/AppSettings.swift`：设置、映射方案和本地持久化入口。

## 3. Windows 当前已经具备的能力

以下能力不应被误判为“需要从原版重新引入”：

- RC003 连接、ATVV 语音解码、指定音频输出端点和后台桥接；
- 13 个实体按键的单击、同键双击和长按映射；
- 方向、返回和音量等适用动作的按住重复；
- 自定义键盘组合键录入；
- 打开 Codex、Claude、cmux、微信、Cursor、Slack、企业微信、网易云音乐、
  Chrome、Edge、Zed 和无线麦设置等固定应用动作；
- 蓝牙、音频、HID tap、VB-CABLE、桥接进程和日志等 Windows 专属诊断；
- 托盘入口、设置窗口和桥接进程分离；
- 音频增益已经是配置值，当前默认 `gain_db=10.0`，但尚未在设置界面开放。

Windows 当前没有：本地使用统计、独立测试音按钮、任意 App 选择、映射方案、
按前台 App 自动切换、组合动作、文字回眸、MCP 读取、手机/网页接力和自动更新。

## 4. 功能评估与当前决策

### 4.1 原作者式设置框架与连接就绪摘要（明确考虑）

当前操作：用户在“连接”“权限”“检查与修复”之间来回查找设备、音频端点、
桥接状态和缺失前提。

建议变化：网页原型先采用左侧导航、单页大标题和连接页就绪摘要；继续保留
Windows 的“连接、按键、权限、检查与修复”语义，但把当前设备、桥接、语音
输出和关键缺项集中显示。只在缺项所在位置提供对应操作，不改变后台行为。

实际影响：用户能先看懂“现在能不能用、缺哪一步”，不用把进程存活误认为
设备和语音链路已经正常。

新增成本：需要重新组织 QML 页面和共享控件，并覆盖四个页面、不同设备模式、
窗口尺寸、主题、错误和未保存状态。它是共享 UI 的 L2 工作，但不必修改配置
schema 或桥接协议。

建议：第一优先级，先用独立网页原型比较方案，再决定正式 QML 信息架构。

### 4.2 连接页三步就绪检查（明确考虑）

建议只显示三项真实状态：

1. 已选择并找到目标设备；
2. 已选择且能打开语音输出端点；
3. 后台桥接正在运行。

“输入法已经能识别文字”不能由程序自动判定，必须继续标为手动验证，不能用
绿色勾选伪装成已完成。

这一功能可以优先复用现有设置控制器和诊断结果，不新增“引导已完成”持久化，
避免仅为页面进度再创建一套状态来源。

建议：与界面框架一起引入。

### 4.3 向当前选定端点发送 1 秒测试音（明确考虑）

当前问题：Windows 能预检端点是否可打开，但用户仍难以区分“端点可打开”和
“VB-CABLE 到目标输入法的完整路由正确”。

建议变化：在连接页加入“发送 1 秒测试音”，只向当前明确选择的输出端点播放
固定、受限音量的测试信号；播放期间禁止重复启动，切换设备、关闭窗口或启动
真实语音时必须可靠停止。

用户以后操作：先选择端点，再在目标输入法或录音工具中观察是否收到测试音，
无需先连接 RC003 或按实体话筒键。

好处：能独立验证 Windows 音频路由，显著缩短 VB-CABLE 排障链路。

新增成本：需要测试音生成、播放资源所有权、与桥接进程并发的端点占用规则，
以及“测试音通过不等于 RC003 语音通过”的明确提示。

建议：第二优先级；价值高，边界清楚，先做单独设计和自动测试。

### 4.4 本地使用统计（待决策）

建议第一阶段只记录：

- 每个逻辑按键成功执行的次数；
- 每次 RC003 语音会话的开始、结束和持续时间；
- 按日、周、全部范围汇总；
- 数据仅保存在 `%LOCALAPPDATA%\RemoteMic\RC003`，不记录语音内容、前台窗口
  标题、蓝牙地址、设备路径或用户输入内容。

用户能看到实际使用频率和语音时长，也能帮助判断某个实体键是否稳定触发。

新增成本：桥接进程是事件生产者，设置窗口是读取者，需要独立、原子、可恢复的
统计存储，不能让高频按键回调直接同步写磁盘。还要设计保留周期、清空入口、
损坏恢复和跨版本格式。因为引入新的持久数据路径，属于 L3，不能与纯界面调整
混在同一批实施。

当前判断：具备价值，但涉及新的持久数据路径和 L3 影响。是否立项、记录范围、
保留周期和清空方式均待用户后续决策。

### 4.5 关于与帮助页（明确考虑）

第一阶段可以提供版本号、项目主页、许可证/第三方说明、日志目录、安装与使用
说明入口，以及“不保存语音内容、不自动修改默认音频设备”的实际边界。

暂不加入自动更新、预发布通道、Dock 设置等 macOS 专属或尚无 Windows 发布基础
的功能。

建议：可随正式界面重排一起引入，成本和风险较低。

## 5. 技术上可以引入，但当前待决策

### 5.1 任意 App 动作（待决策）

Windows 已经支持一组固定 App 动作，因此可以继续扩展为用户选择本机 `.exe`
或快捷方式，并保存明确路径。相比继续增加固定名单，这更符合实际使用。

但它会引入文件选择、路径持久化、程序升级后路径失效、参数注入、网络路径、
签名和不存在目标的处理。第一版应只允许用户主动选择本机现有可执行文件或
快捷方式，不接受任意命令行、脚本、参数和工作目录。

建议：在界面结构稳定后单独引入；不与“任意脚本执行”合并。

### 5.2 可调语音增益（待决策）

Windows 已经通过配置和解码链支持增益，技术上可以增加滑块。问题不是能否
调，而是过高增益会放大噪声并造成削波，用户还需要测试音或真实语音反馈才能
判断数值。

建议先完成测试音和实际电平验证，再评估是否开放有限范围，例如保留当前
`10 dB` 默认、提供重置，并明确它只调整 RC003 解码后的输出，不调整系统麦克风
和 DJI Mic 2。不要直接照搬 macOS 当前显示范围。

### 5.3 多套按键映射方案（待决策）

建议拆成两阶段：

1. 先支持用户手动创建、命名和切换完整映射方案；
2. 再评估按前台 App 自动匹配。

第一阶段已经需要配置 schema、默认方案、删除与重命名、热加载和旧配置迁移；
第二阶段还需要可靠识别前台进程、处理管理员权限差异、UU 远程场景、应用升级
和未匹配时回退。两阶段都属于 L3。

建议：有明确“同一遥控器在多个 App 中需要完全不同键位”的真实需求后再做；
不要为了还原原版截图提前加入空入口。

### 5.4 组合动作（待决策）

原版的组合动作是按顺序执行多个受支持步骤，不等于任意脚本。Windows 若引入，
第一阶段也应只允许已有安全动作的有限序列，并定义延迟、取消、失败停止、按键
松开、重复触发和配置热更新规则。

它会放大输入注入、应用启动、窗口焦点和长按状态之间的竞态。当前单动作、双击、
长按和语音链路尚有真机验收项，不适合现在叠加组合动作。

建议：后置到统计、任意 App 动作和映射方案稳定之后。

## 6. 存在重要前置条件，当前待决策

### 6.1 文字回眸（待决策）

macOS 原版能通过辅助功能读取目标输入控件在语音会话前后的文字快照，再保存
确认新增的文本。Windows 当前只负责把音频送到第三方输入法和注入快捷键，无法
可靠知道最终识别文字，也不拥有目标输入框内容。

若强行实现，需要 UI Automation、剪贴板监听、键盘记录或自带 ASR 等新边界，
会增加权限、兼容、隐私和误记录风险。当前不应仅为了对齐原版功能而引入。

### 6.2 本地 MCP 访问（待决策）

MCP 读取依赖可信、可管理的回眸数据。Windows 当前没有可靠文字来源，因此不能
先做 MCP 外壳或空授权页。只有回眸本身证明可行并完成隐私设计后才有讨论基础。

### 6.3 手机、Apple Watch 或网页接力（待决策）

这些能力需要新的网络协议、身份确认、信任设备、音频传输、服务器或独立客户端，
也会扩大防火墙、凭据和远程执行边界。它们不是 Windows RC003 当前核心问题的
低成本延伸。

建议保持在当前 Windows 仓库范围之外，除非以后有独立产品需求、协议和多仓库
协作计划。

### 6.4 多只 RC003 自动选择（待决策）

Windows 当前在发现多个同名候选时拒绝猜测，随后只在唯一可用候选成立时连接，
这是避免连错设备的安全边界。原版界面中的多设备卡片不能直接移植成自动选择。

只有出现真实多遥控器需求，并能提供不泄漏设备标识的稳定选择和保存方式时，
才重新评估显式设备选择。

### 6.5 自动更新（待决策）

Windows 当前仍是未签名候选，发布来源、签名、更新清单、失败回退和防降级机制
尚未形成稳定合同。现在增加自动更新入口会制造错误安全感。

建议在 Windows 正式签名和发布流程稳定后另行设计，不随关于页一起实现。

## 7. 当前承接边界

1. 网页原型明确考虑界面框架、连接就绪摘要和关于页；
2. “发送 1 秒测试音”进入功能方案考虑，但在实施前仍需单独确认播放条件、
   端点占用、取消方式、提示文字和验证范围；
3. 本地统计、任意 App、增益调节、映射方案、组合动作、回眸、MCP、手机/网页
   接力、多设备选择和自动更新只保留现状分析，全部待用户后续决策；
4. 本次记录不授权修改正式 QML、配置 schema、桥接运行时或发布产物。

## 8. Windows 相关源码入口

- `src/ovb_rc003/qml/main.qml`：当前四页导航和全局状态栏；
- `src/ovb_rc003/qml/ConnectionPage.qml`：设备、输出端点和桥接启动；
- `src/ovb_rc003/qml/ButtonsPage.qml`：13 键映射、手势和快捷键录入；
- `src/ovb_rc003/qml/PermissionsPage.qml`：Windows 权限与人工设置入口；
- `src/ovb_rc003/qml/DiagnosticsPage.qml`：自动检测、VB-CABLE 和系统入口；
- `src/ovb_rc003/qt_settings_app.py`：QML 控制器、保存、诊断和桥接启动；
- `src/ovb_rc003/key_mapping.py`：动作、手势与配置语义；
- `src/ovb_rc003/action_executor.py`：固定 App 动作解析和启动；
- `src/ovb_rc003/config.py`：配置 schema 与当前 `gain_db`；
- `src/ovb_rc003/audio_playback.py`：指定端点播放和资源所有权；
- `src/ovb_rc003/app.py`：桥接事件、动作执行和语音会话。
